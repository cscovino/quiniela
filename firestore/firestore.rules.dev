rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules - allow all operations locally
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
