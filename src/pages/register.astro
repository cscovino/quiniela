---
import BaseLayout from '@layouts/BaseLayout.astro';
import AuthPage from '@pages/AuthPage.astro';
import es from '@locales/es/auth.json';

const locale = 'es';
---

<BaseLayout locale={locale} title={es.auth.register.title} description={es.auth.register.subtitle}>
  <AuthPage locale={locale} initialMode="register" />
</BaseLayout>
