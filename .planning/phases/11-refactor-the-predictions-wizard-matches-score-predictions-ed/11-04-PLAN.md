---
phase: 11-refactor-the-predictions-wizard-matches-score-predictions-ed
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/locales/en/common.json
  - src/locales/es/common.json
autonomous: true
requirements: [G]
user_setup: []

must_haves:
  truths:
    - "All prediction wizard UI text is translated in both English and Spanish"
    - "No hardcoded Spanish strings remain in component code"
    - "Hardcoded English fallback strings are replaced with locale key references"
    - "Round labels (Round of 32, Round of 16, Quarterfinals, etc.) use locale keys"
    - "Form labels (pick winner, teams TBD, submit, editing) use locale keys"
  artifacts:
    - path: "src/locales/en/common.json"
      provides: "All missing predictions keys added in English"
      changes:
        - "Added keys: hintFillAllMatches, hintRankAllTeams, submitGroupCombined, submitting, pickWinner, teamsTbd, submitRound, allSubmitted"
        - "Added keys: roundOf32, roundOf16, quarterfinals, semifinals, thirdPlace, final"
        - "Added keys: editing match-prediction labels"
    - path: "src/locales/es/common.json"
      provides: "All missing predictions keys added in Spanish"
      changes:
        - "Same keys as en/common.json with Spanish translations"
  key_links:
    - from: "en/common.json predictions"
      to: "PredictionStepGroup.tsx, PredictionStepKnockoutRound.tsx, PredictionStepWizard.tsx (future)"
      via: "t functions read locale keys for all UI labels"
      pattern: "t\\(['\"](predictions\\.|predictions-step\\.)"
    - from: "es/common.json predictions"
      to: "Same components as en"
      via: "t function resolves Spanish locale for Spanish routes"
      pattern: "t\\(['\"](predictions\\.|predictions-step\\.)"
---

<objective>
**Locale Audit — Complete Predictions Translations**

**Purpose:** Add all missing translations keys for the predictions wizard UI in both English and Spanish. Remove hardcoded strings from components and replace with locale key references.

**Output:** Complete translation coverage for predictions wizard in both locales.

**Scope items:** G (locale audit)
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Source files
@src/locales/en/common.json
@src/locales/es/common.json
@src/components/molecules/Predictions/PredictionStepGroup.tsx
@src/components/molecules/Predictions/PredictionStepKnockoutRound.tsx
@src/components/templates/PredictionsTemplate/PredictionsTemplate.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add missing predictions keys to English locale</name>
  <read_first>src/locales/en/common.json</read_first>
  <files>src/locales/en/common.json</files>
  <action>
    Per D-06 (Locale audit: all predictions UI text in both languages):

    Read the full `predictions` section in en/common.json (starting around line 119). Audit all keys currently present, then add the following missing keys in the appropriate position within the `predictions` object. Maintain alphabetical order within the object to match existing style.

    **Missing keys to add:**

    ```json
    "roundOf32": "Round of 32",
    "roundOf16": "Round of 16",
    "quarterfinals": "Quarter-finals",
    "semifinals": "Semi-finals",
    "thirdPlace": "Third place play-off",
    "final": "Final",
    "hintFillAllMatches": "Fill in all match predictions to continue",
    "hintRankAllTeams": "Rank all teams in the group to continue",
    "submitGroupCombined": "Submit & Continue",
    "submitting": "Saving...",
    "pickWinner": "Pick winner",
    "teamsTbd": "Teams TBD",
    "submitRound": "Save round predictions",
    "allSubmitted": "All knockout predictions saved",
    "editingLabel": "Editing predictions for {step}",
    "unsavedChanges": "You have unsaved changes",
    "predictionsSaved": "Predictions saved successfully",
    "saveError": "Could not save predictions. Please try again."
    ```

    **Key naming:** All use `camelCase` matching existing pattern. The `editingLabel` uses `{step}` interpolation consistent with existing i18n patterns.

    **Interpolation note:** If the existing locale files use `react-i18next` interpolation syntax, use `{{step}}` instead of `{step}`. Read the existing file to determine interpolation syntax and match it.

    **Verify existing keys:** Before adding, grep for each key to confirm it doesn't already exist to avoid duplicates.
  </action>
  <verify>
    <automated>
      grep "roundOf32" src/locales/en/common.json && echo "EXISTS"
    </automated>
    <acceptance_criteria>
      - All required keys exist in en/common.json predictions section
      - Keys are in alphabetical order matching existing convention
      - Interpolation syntax matches existing pattern ({{var}} or {var})
      - No duplicate keys created
    </acceptance_criteria>
  </verify>
  <done>
    English locale file has complete predictions wizard translation keys.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add missing predictions keys to Spanish locale</name>
  <read_first>src/locales/es/common.json</read_first>
  <files>src/locales/es/common.json</files>
  <action>
    Per D-06 (Locale audit: all predictions UI text in both languages):

    Add the same keys from Task 1 to the Spanish locale file with Spanish translations:

    ```json
    "roundOf32": "Treintaidosavos de final",
    "roundOf16": "Octavos de final",
    "quarterfinals": "Cuartos de final",
    "semifinals": "Semifinales",
    "thirdPlace": "Partido por el tercer puesto",
    "final": "Final",
    "hintFillAllMatches": "Completa todos los pronósticos para continuar",
    "hintRankAllTeams": "Ordena todos los equipos del grupo para continuar",
    "submitGroupCombined": "Guardar y continuar",
    "submitting": "Guardando...",
    "pickWinner": "Elige ganador",
    "teamsTbd": "Equipos por definir",
    "submitRound": "Guardar pronósticos de la ronda",
    "allSubmitted": "Todos los pronósticos guardados",
    "editingLabel": "Editando pronósticos para {step}",
    "unsavedChanges": "Tienes cambios sin guardar",
    "predictionsSaved": "Pronósticos guardados correctamente",
    "saveError": "No se pudieron guardar los pronósticos. Inténtalo de nuevo."
    ```

    **Key naming:** Same camelCase keys as English. Maintain existing alphabetical order within the `predictions` object.

    **Interpolation:** Use the same `{{step}}` or `{step}` syntax as the existing Spanish file.

    **Verify existing keys:** Confirm none of these keys already exist in es/common.json before adding.
  </action>
  <verify>
    <automated>
      grep "roundOf32" src/locales/es/common.json && echo "EXISTS"
    </automated>
    <acceptance_criteria>
      - All required keys exist in es/common.json predictions section
      - Keys are in alphabetical order matching existing convention
      - Interpolation syntax matches existing pattern
      - Translations are natural Spanish (not literal English translations)
    </acceptance_criteria>
  </verify>
  <done>
    Spanish locale file has complete predictions wizard translation keys.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Replace hardcoded string literals in components with locale key references</name>
  <read_first>
    src/components/molecules/Predictions/PredictionStepGroup.tsx
    src/components/molecules/Predictions/PredictionStepKnockoutRound.tsx
    src/components/templates/PredictionsTemplate/PredictionsTemplate.tsx
  </read_first>
  <files>
    src/components/molecules/Predictions/PredictionStepGroup.tsx,
    src/components/molecules/Predictions/PredictionStepKnockoutRound.tsx,
    src/components/templates/PredictionsTemplate/PredictionsTemplate.tsx
  </files>
  <action>
    Per D-06 (Locale audit: replace hardcoded strings with t() calls):

    **In PredictionStepGroup.tsx:**
    1. Find and replace any hardcoded strings in JSX that relate to:
       - "Fill in all match predictions to continue" → `t('predictions.hintFillAllMatches')`
       - "Rank all teams in the group to continue" → `t('predictions.hintRankAllTeams')`
       - Save/Submit button labels → `t('predictions.submitGroupCombined')`
       - Any remaining strings in the component's rendered output

    2. Verify `t` function is imported (likely from `useTranslation` hook). If the component doesn't use `t` yet, add it:
       ```typescript
       import { useTranslation } from 'react-i18next';
       ```
       And inside the component: `const { t } = useTranslation();`

    **In PredictionStepKnockoutRound.tsx:**
    1. Find and replace hardcoded strings:
       - "Pick winner" → `t('predictions.pickWinner')`
       - "Teams TBD" → `t('predictions.teamsTbd')`
       - "Save round predictions" (or equivalent button text) → `t('predictions.submitRound')`
       - "All knockout predictions saved" (or equivalent confirmation) → `t('predictions.allSubmitted')`
       - Any round labels like "Round of 32", "Round of 16", "Quarter-finals" → use `t('predictions.roundOf32')` etc.

    2. Verify/add `t` import if needed.

    **In PredictionsTemplate.tsx:**
    1. Find and replace hardcoded strings:
       - Any step labels that hardcode round names → use `t(…)` with locale keys
       - "Editing predictions for {step}" type text → `t('predictions.editingLabel', { step: ... })`
       - Loading/error/success messages → use new locale keys
       - "You have unsaved changes" → `t('predictions.unsavedChanges')`
       - Save success/error toasts → use `t('predictions.predictionsSaved')` / `t('predictions.saveError')`

    2. Import `useTranslation` if not already present.

    **Important:** Do NOT change programmatic identifiers, API strings, or Firestore document field names. Only replace user-visible text strings.

    After each replacement, run lint to verify no import issues.
  </action>
  <verify>
    <automated>
      pnpm lint -- --max-warnings 0 2>&1 | tail -5
    </automated>
    <acceptance_criteria>
      - No hardcoded user-visible strings remain in PredictionStepGroup.tsx
      - No hardcoded user-visible strings remain in PredictionStepKnockoutRound.tsx
      - No hardcoded user-visible strings remain in PredictionsTemplate.tsx
      - All replaced strings use `t('predictions.KEY')` pattern
      - Lint passes with 0 warnings
    </acceptance_criteria>
  </verify>
  <done>
    All three components use locale keys via t() for all user-visible text. Hardcoded strings replaced. Locale files have complete key coverage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Locale files — no security boundary. Translation keys and values are static data bundled at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Tampering | locale JSON files | accept | Static build assets; no user input affects locale values |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
1. `pnpm lint -- --max-warnings 0` — zero lint errors/warnings
2. `pnpm build` — production build succeeds
3. `pnpm test:run` — all existing tests pass
4. Manual: grep for user-visible English strings in PredictionStepGroup.tsx — remaining strings are programmatic (Firestore fields, etc.) not display text
5. Manual: grep for user-visible English strings in PredictionStepKnockoutRound.tsx — same
6. Manual: grep for user-visible English strings in PredictionsTemplate.tsx — same
7. Open app at /es/predictions route — all predictions text in Spanish
8. Open app at /en/predictions route — all predictions text in English
</verification>

<success_criteria>
- Both en/common.json and es/common.json have complete predictions section key coverage
- All hardcoded user-visible string literals in prediction wizard components replaced with t() calls
- English and Spanish locales are in sync (same keys, different values)
- Build succeeds with no missing key errors
</success_criteria>

<output>
Create `.planning/phases/11-refactor-the-predictions-wizard-matches-score-predictions-ed/11-04-SUMMARY.md` when done
</output>
