---
phase: 11-refactor-the-predictions-wizard-matches-score-predictions-ed
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/molecules/Predictions/PredictionStepKnockoutRound.tsx
  - src/utils/predictions-flow.ts
autonomous: true
requirements: [A, F]
user_setup: []

must_haves:
  truths:
    - "Submitted knockout bets are editable (not locked) before deadline"
    - "Knockout bracket shows correct team assignments in editor mode"
    - "Submitted knockout matches appear as editable (not read-only) before deadline"
  artifacts:
    - path: "src/components/molecules/Predictions/PredictionStepKnockoutRound.tsx"
      provides: "Editable knockout bets, correct team assignments from resolved bracket"
      changes:
        - "isMatchDisabled: remove unconditional lock for existing knockout bets"
    - path: "src/utils/predictions-flow.ts"
      provides: "BRACKET_MAP correctness fix and buildKnockoutBracket debugging/investigation"
      changes:
        - "BRACKET_MAP or resolveSlot/bracket building functions corrected for editor mode"
  key_links:
    - from: "PredictionStepKnockoutRound.tsx"
      to: "predictions-flow.ts BRACKET_MAP"
      via: "resolvedMatches uses BRACKET_MAP for team slot resolution"
      pattern: "BRACKET_MAP\\[match.slug\\]"
    - from: "PredictionStepKnockoutRound.tsx"
      to: "prediction-service.ts submitBatchKnockoutBets"
      via: "onSubmit calls service with merge support (Plan 01)"
      pattern: "submitBatchKnockoutBets"
---

<objective>
**Editable Scores — Knockout Form & Bracket Fix**

**Purpose:** Enable editing already-submitted knockout predictions before the deadline by removing the existing-bet lock in the knockout UI. Investigate and fix incorrect knockout bracket team assignments in editor mode.

**Output:** Updated knockout round component with correct disabled logic, fixed bracket team resolution.

**Scope items:** A (editable scores: knockout), F (editor knockout bracket fix)
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-refactor-the-predictions-wizard-matches-score-predictions-ed/11-01-PLAN.md

# Source files
@src/components/molecules/Predictions/PredictionStepKnockoutRound.tsx
@src/utils/predictions-flow.ts
@src/components/molecules/Predictions/PredictionStepGroup.tsx
@src/components/templates/PredictionsTemplate/PredictionsTemplate.tsx
@src/hooks/usePredictionSteps.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix isMatchDisabled in PredictionStepKnockoutRound for editable knockout bets</name>
  <read_first>
    src/components/molecules/Predictions/PredictionStepKnockoutRound.tsx
    src/components/molecules/Predictions/PredictionStepGroup.tsx
  </read_first>
  <files>src/components/molecules/Predictions/PredictionStepKnockoutRound.tsx</files>
  <action>
    Per D-01 (Score editing scope: Edit already-submitted scores until deadline):

    1. **Fix knockout round rendering to include submitted matches:** Currently, the component hides submitted matches entirely (they only show in the submitted summary). For editable knockout bets, submitted matches must remain visible and editable until deadline. As a minimum viable change: if `existingKnockoutBets.has(m.slug)`, still render the match as editable (not locked) as long as deadline hasn't passed.

    2. **`isMatchDisabled` function (lines 197-202):** Remove the unconditional timeout-based deadline check (line 200: `if (match.predictionDeadline.getTime() <= now) return true;`). Use the same approach as PredictionStepGroup: check `match.predictionDeadline` against `Date.now()` for actual deadline comparison. The function must only return true when:
       - `isDisabled || isSubmitting` (global disable)
       - `!match.homeTeam || !match.awayTeam` (teams not yet determined)
       - match deadline has actually passed: `match.predictionDeadline.getTime() <= now`

       IMPORTANT: Do NOT add a check for `existingKnockoutBets.has(match.slug)` — submitted knockout bets should remain editable before deadline, consistent with the group form fix.

    3. **Make submitted matches editable in the render loop (lines 218-270):** The current render only iterates `resolvedMatches` (which only covers `unsubmittedMatches`). Submitted matches are hidden behind a summary badge. With editable scores, submitted matches should still appear in the editable match list. 
    
       Approach: Merge `resolvedMatches` with the submitted matches. For each submitted match, resolve its current teams from `existingKnockoutBets` + `groupBetsByGroupId` and render it identically to unsubmitted matches. The `disabled` state determines whether the score inputs are locked (based on deadline), not submission status.

       Alternatively: Render all matches (submitted + unsubmitted) in a single unified list, removing the "Submitted predictions:" summary section in favor of inline editable controls. Apply `isMatchDisabled` to determine editability per-match.

    4. **Fix `predictions` state initialization:** When `existingKnockoutBets` contains a match slug, pre-seed the `predictions` state with the existing bet value so the TeamSelector reflects the current saved choice. Use an `initialPredictions` derived from `previousRoundPredictions` and `existingKnockoutBets` to hydrate the `useState`.
  </action>
  <verify>
    <automated>
      pnpm test:run 2>&1 | tail -10
    </automated>
    <acceptance_criteria>
      - `isMatchDisabled` does NOT contain unconditional deadline check (no `return true` on deadline without context)
      - `isMatchDisabled` does NOT check `existingKnockoutBets`
      - Submitted knockout matches render inline (not just in summary badge) when deadline hasn't passed
      - `predictions` useState initializes with existing knockout bet values for already-submitted matches
      - All existing tests pass
    </acceptance_criteria>
  </verify>
  <done>
    PredictionStepKnockoutRound allows editing submitted knockout bets before deadline. Submitted matches render as editable inline items. Existing bet values pre-fill the TeamSelector. All existing tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Investigate and fix incorrect knockout bracket team assignments in editor mode</name>
  <read_first>
    src/utils/predictions-flow.ts
    src/hooks/usePredictionSteps.tsx
    src/components/molecules/Predictions/PredictionStepKnockoutRound.tsx
    src/components/templates/PredictionsTemplate/PredictionsTemplate.tsx
  </read_first>
  <files>src/utils/predictions-flow.ts</files>
  <action>
    Per scope item F (Editor knockout bracket — incorrect team assignments):

    1. **Investigate root cause** by examining `BRACKET_MAP` (lines 181-289) and `buildKnockoutBracket` (lines 320-368). The bracket map defines which group positions feed into which R32 slots. The 2026 World Cup format has 48 teams in 12 groups (A-L), with the top 2 from each group + 8 best 3rd-place teams advancing to R32.

    2. **Check R32 mapping sanity:** The current BRACKET_MAP has 8 R32 matches (r32-m1 through r32-m8). Verify each slot's `groupId` and `position` match the official 2026 format:
       - r32-m1: A1 (winner) vs C3 (3rd place)
       - r32-m2: B1 vs D3
       - r32-m3: E1 vs G3
       - r32-m4: F1 vs H3
       - r32-m5: I1 vs K3
       - r32-m6: J1 vs L3
       - r32-m7: A2 vs B2
       - r32-m8: C2 vs D2
       
       Confirm these match the actual tournament format. If group letters skip (e.g., no groups from A to L all used), add missing entries or fix offset.

    3. **Check R16 onward mappings:** Verify `winner-of` references for r16-m1 through r16-m8 correctly follow from R32 winners. The R16 bracket should cross-pair R32 matches in a standard knockout tree. Check that `r16-m5` through `r16-m8` don't duplicate `r16-m1` through `r16-m4` paths.

    4. **Fix issue:** If bracket mappings are wrong, update the problematic `BRACKET_MAP` entries with correct group/position references. If the issue is in `resolveSlot` (lines 292-317) or `buildKnockoutBracket` (lines 320-368), fix those functions.

    5. **Check `PredictionStepKnockoutRound` rendering:** Verify the `resolvedMatches` memo (lines 134-166) applies bracket resolution correctly. The `BRACKET_MAP` resolution happens in `PredictionStepKnockoutRound.tsx` via `resolveTeamFromBracket` (lines 72-96) — not in `predictions-flow.ts`'s `buildKnockoutBracket`. Ensure `resolveTeamFromBracket` reads the correct `source` from the bracket entry.

    The exact fix depends on investigation outcome. Common issues:
    - Group IDs in BRACKET_MAP don't match actual group slugs (e.g., "A" vs "group-a")
    - Position indices are off-by-one (position 1 = group winner, not position 0)
    - R16 cross-pairing duplicates instead of using proper bracket tree
  </action>
  <verify>
    <automated>
      pnpm test:run 2>&1 | tail -10
    </automated>
    <acceptance_criteria>
      - BRACKET_MAP entries correctly reference existing group slugs from tournament data
      - R32 → R16 → QF → SF → Final knockout tree follows a valid bracket structure
      - `buildKnockoutBracket` returns correctly resolved teams for all knockout matches
      - `resolveTeamFromBracket` in PredictionStepKnockoutRound returns correct teams
      - All existing tests pass
    </acceptance_criteria>
  </verify>
  <done>
    BRACKET_MAP entries verified and fixed for correct tournament format. Bracket slot resolution produces correct team assignments in editor mode. All existing tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Firestore | Knockout bet winner selection crosses from browser to Firestore via batch writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | BRACKET_MAP in predictions-flow.ts | mitigate | Static data — no user input; reviewed against official format |
| T-11-04 | Tampering | knockout bet submission | mitigate | batch.set({merge:true}) with existing security rules; no new surface |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
1. `pnpm test:run` — all existing tests pass
2. `pnpm lint -- --max-warnings 0` — zero lint errors/warnings
3. `pnpm build` — production build succeeds
4. Manual: browse knockout prediction in editor mode — verify team names map to correct group positions
5. Manual: verify a submitted knockout bet shows as editable before deadline
</verification>

<success_criteria>
- Knockout component allows editing submitted bets before deadline (no existing-bet lock)
- Submitted knockout matches render inline as editable (not hidden in summary badge)
- Knockout bracket team assignments in editor mode match official tournament format
- BRACKET_MAP correctly references all 12 groups (A-L) with correct positions
- R16 onward follow valid bracket tree structure
</success_criteria>

<output>
Create `.planning/phases/11-refactor-the-predictions-wizard-matches-score-predictions-ed/11-02-SUMMARY.md` when done
</output>
