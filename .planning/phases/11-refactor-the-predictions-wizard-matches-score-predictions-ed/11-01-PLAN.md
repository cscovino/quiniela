---
phase: 11-refactor-the-predictions-wizard-matches-score-predictions-ed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/prediction-service.ts
  - src/components/molecules/Predictions/PredictionStepGroup.tsx
autonomous: true
requirements: [A, B, I]
user_setup: []

must_haves:
  truths:
    - "Submitted match scores are editable (not locked) before deadline"
    - "Unfilled score inputs show empty/null (not forced 0-0)"
    - "A step is incomplete unless all scores are explicitly filled"
    - "Submitted-but-editable matches don't get injected with {home:0, away:0} in allPredictions"
    - "Group heading does not render twice (no duplicate h2)"
  artifacts:
    - path: "src/services/prediction-service.ts"
      provides: "Batch submit methods allow overwriting existing bets with setDoc({merge:true})"
      exports:
        - submitBatchMatchBets
        - submitBatchGroupBets
        - submitBatchKnockoutBets
      changes:
        - "validateMatchBet: remove existing-bet rejection (lines 52-58)"
        - "submitBatchMatchBets batch.set → uses {merge:true} + updatedAt"
        - "submitBatchGroupBets batch.set → uses {merge:true} + updatedAt"
        - "submitBatchKnockoutBets batch.set → uses {merge:true} + updatedAt"
    - path: "src/components/molecules/Predictions/PredictionStepGroup.tsx"
      provides: "isMatchDisabled, allPredictions fixes, duplicate header fix"
      changes:
        - "isMatchDisabled: remove `existingMatchBets.has(matchId)` return (line 186)"
        - "allPredictions: remove `combined[match.id] = {home:0, away:0}` loop (lines 92-94)"
        - "allPredictions: submitted matches only contribute via matchPredictions edits, not defaults"
        - "Duplicate h2: change component heading from variant=\"h2\" to variant=\"h3\" (line 197)"
  key_links:
    - from: "PredictionStepGroup.tsx isMatchDisabled"
      to: "prediction-service.ts submitBatchMatchBets"
      via: "removed existing-bet lock allows resubmission with {merge:true}"
      pattern: "submitBatchMatchBets.*merge"
---

<objective>
**Editable Scores — Service Layer & Group Form Fixes**

**Purpose:** Enable editing already-submitted match predictions until the prediction deadline. Remove the unconditional lock on submitted scores in both validation (service) and UI (component). Fix the default-draw bug that injects `{home:0, away:0}` for submitted-but-editable matches. Fix duplicate group heading.

**Output:** Editable match predictions service, updated group form component with correct disabled logic, null-default predictions, and single group heading.

**Scope items:** A (editable scores: service + group), B (default draw fix), I (duplicate group header)
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Source files
@src/services/prediction-service.ts
@src/components/molecules/Predictions/PredictionStepGroup.tsx
@src/types/firestore.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update prediction-service.ts for editable bets (batch methods + validation)</name>
  <read_first>src/services/prediction-service.ts</read_first>
  <files>src/services/prediction-service.ts</files>
  <action>
    Per D-01 (Score editing scope: Edit already-submitted scores until deadline) and D-02 (Default draw: Null = incomplete):

    1. **`validateMatchBet` (lines 30-61):** Remove the existing-bet rejection block (lines 52-58). The function must only check:
       - Match exists
       - match.status !== 'finished' && match.status !== 'live'
       - match.predictionDeadline not passed
       Allow overwriting regardless of existing bet existence.

    2. **`validateKnockoutBet` (lines 63-98):** Same treatment — remove existing-bet rejection (lines 89-95). Keep match existence, status checks, and team availability checks.

    3. **`validateGroupBet` (lines 100-125):** Remove existing-bet rejection (lines 116-122). Keep match status checks (no live/finished matches in group).

    4. **`submitBatchMatchBets` (lines 242-323):** Change `batch.set(betRef, {...})` (line 278) to use `batch.set(betRef, {...}, { merge: true })`. Ensure `updatedAt: serverTimestamp()` is always sent even on updates. Keep the `createdAt` field so it's preserved on merge. The `updatedAt` must be set in the data object passed to `setDoc`.

    5. **`submitBatchGroupBets` (lines 325-369):** Change `batch.set(betRef, {...})` (line 344) to `batch.set(betRef, {...}, { merge: true })` with `updatedAt: serverTimestamp()`.

    6. **`submitBatchKnockoutBets` (lines 371-428):** Change `batch.set(betRef, {...})` (line 404) to `batch.set(betRef, {...}, { merge: true })`. Knockout bets currently lack `updatedAt` — add it (type KnockoutBet in firestore.ts may need `updatedAt?: Timestamp` field added — add it as optional to avoid breaking existing data).

    7. **Import check:** Confirm `serverTimestamp` is already imported (line 8) — it is. No new imports needed.

    Export signatures remain unchanged — all existing callers continue to work.
  </action>
  <verify>
    <automated>
      pnpm lint -- --max-warnings 0 2>&1 | tail -5
    </automated>
    <acceptance_criteria>
      - `validateMatchBet` no longer references `existingBetSnap.exists()`
      - `validateKnockoutBet` no longer references `existingBetSnap.exists()`
      - `validateGroupBet` no longer references `existingBetSnap.exists()`
      - `submitBatchMatchBets` calls `batch.set(ref, data, { merge: true })`
      - `submitBatchGroupBets` calls `batch.set(ref, data, { merge: true })`
      - `submitBatchKnockoutBets` calls `batch.set(ref, data, { merge: true })`
      - `KnockoutBet` type has optional `updatedAt?: Timestamp`
      - Lint passes with 0 warnings
    </acceptance_criteria>
  </verify>
  <done>
    prediction-service.ts allows overwriting existing bets via merge. All three batch methods use merge pattern. Validation functions no longer reject existing bets. KnockoutBet type has updatedAt.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix PredictionStepGroup — isMatchDisabled, allPredictions, duplicate heading</name>
  <read_first>src/components/molecules/Predictions/PredictionStepGroup.tsx</read_first>
  <files>src/components/molecules/Predictions/PredictionStepGroup.tsx</files>
  <action>
    Per D-01, D-02, and scope items A, B, I:

    1. **`isMatchDisabled` function (lines 184-192):** Remove line `if (existingMatchBets.has(matchId)) return true;` (line 186). The function must now only return true when:
       - `isDisabled || isSubmitting` (global disable)
       - match not found
       - `match.status === 'finished' || match.status === 'live'`
       - `match.predictionDeadline` passed
       Submitted matches are now editable until deadline — remove the existing-bet blocking check entirely.

    2. **`allPredictions` memo (lines 90-101):** Remove the `for (const match of submittedMatches)` loop (lines 92-94) that injects `combined[match.id] = { home: 0, away: 0 }`. Submitted matches should NOT contribute implicit 0-0 to the predictions record. They should only appear if the user has explicit edits in `matchPredictions`. The second loop (lines 95-99) already handles edited entries.

    3. **Duplicate heading fix:** The template (PredictionsTemplate.tsx line 403) renders `<Typography variant="h2">{activeStep.label}</Typography>` for the step label. PredictionStepGroup's own heading (line 197) is redundant as h2. Change `variant="h2"` to `variant="h3"` on line 197 in the `prediction-step-group__header` div.

    No imports need to change — `Typography` is already imported.
  </action>
  <verify>
    <automated>
      pnpm test:run 2>&1 | tail -10
    </automated>
    <acceptance_criteria>
      - `isMatchDisabled` does NOT contain `existingMatchBets.has(matchId)` 
      - `allPredictions` memo does NOT contain a loop over `submittedMatches` injecting home:0/away:0
      - Heading in `.prediction-step-group__header` uses `variant="h3"` (not h2)
      - All existing tests pass
    </acceptance_criteria>
  </verify>
  <done>
    PredictionStepGroup allows editing submitted matches before deadline. Unfilled scores show null (not 0-0). Group heading uses h3 to avoid duplicating template's h2. All existing tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Firestore | Unvalidated score values cross from browser to Firestore via batch writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | prediction-service.ts submitBatchMatchBets | mitigate | Firestore write via batch.set({merge:true}) — existing security rules still enforce auth + per-user write; no new surface |
| T-11-02 | Information Disclosure | reCAPTCHA V3 | accept | reCAPTCHA V3 already configured in production; no PII exposed |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan |
</threat_model>

<verification>
1. `pnpm test:run` — all existing tests pass
2. `pnpm lint -- --max-warnings 0` — zero lint errors/warnings
3. `pnpm build` — production build succeeds
4. Manual: grep `existingMatchBets.has(matchId)` returns only false positives (comments)
5. Manual: grep `{ home: 0, away: 0 }` or `home: 0, away: 0` in PredictionStepGroup returns no results
6. Manual: grep `variant="h2"` in PredictionStepGroup.tsx returns no results (heading now h3)
</verification>

<success_criteria>
- Service layer allows overwriting existing bets for all match/group/knockout types via merge pattern
- Validation no longer rejects previously-submitted predictions (only deadline/status checks remain)
- PredictionStepGroup allows score editing for submitted matches before deadline
- Unfilled matches don't get implicit 0-0 in predictions record
- Group heading uses h3 (not h2) — no duplicate with template label
</success_criteria>

<output>
Create `.planning/phases/11-refactor-the-predictions-wizard-matches-score-predictions-ed/11-01-SUMMARY.md` when done
</output>
