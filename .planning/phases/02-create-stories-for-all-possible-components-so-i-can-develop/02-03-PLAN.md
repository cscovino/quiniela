---
phase: 02-stories
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/molecules/LoginForm/LoginForm.stories.tsx
  - src/components/molecules/RegisterForm/RegisterForm.stories.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "LoginForm story shows email/password form with Google login button"
    - "LoginForm story shows password reset flow"
    - "LoginForm story shows error state"
    - "RegisterForm story shows registration form with all fields"
    - "RegisterForm story shows password mismatch validation"
  artifacts:
    - path: "src/components/molecules/LoginForm/LoginForm.stories.tsx"
      provides: "LoginForm stories covering default, reset, and error states"
    - path: "src/components/molecules/RegisterForm/RegisterForm.stories.tsx"
      provides: "RegisterForm stories covering default and validation states"
  key_links:
    - from: "LoginForm.stories.tsx"
      to: "LoginForm.tsx"
      via: "import"
      pattern: "import.*LoginForm"
    - from: "RegisterForm.stories.tsx"
      to: "RegisterForm.tsx"
      via: "import"
      pattern: "import.*RegisterForm"
---

<objective>
Create Storybook stories for the auth-form molecules: LoginForm and RegisterForm.

**Purpose:** LoginForm and RegisterForm are gateways to the app. Stories verify form layout, field validation, and all interaction states (loading, errors, password reset flow) without needing Firebase auth setup.
**Output:** 2 new `.stories.tsx` files
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/molecules/LoginForm/LoginForm.tsx
@src/components/molecules/RegisterForm/RegisterForm.tsx
@src/components/molecules/TeamSelector/TeamSelector.stories.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LoginForm stories</name>
  <files>
    src/components/molecules/LoginForm/LoginForm.stories.tsx
  </files>
  <action>
    Create `LoginForm.stories.tsx` following the project pattern (`type Meta, StoryObj from @storybook/react-vite`, `tags: ['autodocs']`).

    LoginForm requires:
    - `translations` object with nested `errors` and `success` objects (see component interface)
    - `onRegisterClick` callback — use `action('register-click')` from `@storybook/addon-actions`

    Build a `defaultTranslations` object with realistic Spanish or English text for all required keys.

    Stories:
    - **Default:** Shows the login form with email, password fields, Google login button, and "Forgot password?" link
    - **Loading:** Mock `useAuthStore.setState({ isLoading: true })` — shows loading spinner on submit button
    - **WithError:** Mock `useAuthStore.setState({ error: 'wrong-password' })` — shows error message above form
    - **ShowReset:** Create a wrapper component that initializes with `showReset=true` inline state, or use initial state injection — shows the password reset flow with email field
    - **ResetSent:** Wrapper that initializes with `resetSent=true` — shows success message after reset email sent
  </action>
  <verify>
    <automated>pnpm build-storybook 2>&1 | tail -3</automated>
  </verify>
  <done>All LoginForm stories render in Storybook</done>
</task>

<task type="auto">
  <name>Task 2: Create RegisterForm stories</name>
  <files>
    src/components/molecules/RegisterForm/RegisterForm.stories.tsx
  </files>
  <action>
    Create `RegisterForm.stories.tsx` following the same pattern.

    RegisterForm requires:
    - `translations` object with nested `errors` (see component interface)
    - `onLoginClick` callback — use `action('login-click')`

    Build a `defaultTranslations` object with realistic text for all required keys.

    Stories:
    - **Default:** Shows the registration form with displayName, email, password, confirmPassword fields and Google login button
    - **Loading:** Mock `useAuthStore.setState({ isLoading: true })` — shows loading on submit button
    - **WithError:** Mock `useAuthStore.setState({ error: 'email-already-in-use' })` — shows error message
    - **PasswordMismatch:** Create a decorator or controlled wrapper that sets up state where confirmPassword differs from password — triggers the `passwordMismatch` validation and shows error styling on the confirmPassword Input
  </action>
  <verify>
    <automated>pnpm build-storybook 2>&1 | tail -3</automated>
  </verify>
  <done>All RegisterForm stories render in Storybook</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `pnpm build-storybook` succeeds
- Each story renders in Storybook without errors
</verification>

<success_criteria>
- 2 new `.stories.tsx` files created
- All specified stories render
- No console errors in Storybook
</success_criteria>

<output>
Create `.planning/phases/02-create-stories-for-all-possible-components-so-i-can-develop/02-03-SUMMARY.md` when done
</output>
