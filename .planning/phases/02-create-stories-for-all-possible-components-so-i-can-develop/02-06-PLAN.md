---
phase: 02-stories
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/organisms/AdminMatchesPage/AdminMatchesPage.stories.tsx
  - src/components/organisms/AdminMatchList/AdminMatchList.stories.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "AdminMatchList story renders grouped matches by phase"
    - "AdminMatchList story shows match info with Edit button"
    - "AdminMatchList story renders AdminMatchResultForm when editing"
    - "AdminMatchesPage story renders loading state"
    - "AdminMatchesPage story renders match list"
  artifacts:
    - path: "src/components/organisms/AdminMatchesPage/AdminMatchesPage.stories.tsx"
      provides: "AdminMatchesPage stories for data states"
    - path: "src/components/organisms/AdminMatchList/AdminMatchList.stories.tsx"
      provides: "AdminMatchList stories for match display"
  key_links:
    - from: "AdminMatchesPage.stories.tsx"
      to: "AdminMatchesPage.tsx"
      via: "import"
    - from: "AdminMatchList.stories.tsx"
      to: "AdminMatchList.tsx"
      via: "import"
---

<objective>
Create Storybook stories for admin components: AdminMatchesPage and AdminMatchList.

**Purpose:** Admin components manage match results and tournament data. Stories let the admin panel be developed and tested independently of Firebase/Firestore data loading, with mocked match data.
**Output:** 2 new `.stories.tsx` files
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/organisms/AdminMatchesPage/AdminMatchesPage.tsx
@src/components/organisms/AdminMatchList/AdminMatchList.tsx
@src/components/organisms/MatchList/MatchList.stories.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AdminMatchList stories</name>
  <files>
    src/components/organisms/AdminMatchList/AdminMatchList.stories.tsx
  </files>
  <action>
    Create `AdminMatchList.stories.tsx` with `@storybook/react-vite`.

    AdminMatchList takes `matches: (Match & { id: string })[]` and `onUpdateResult: (matchId, homeScore, awayScore, status) => void`.

    Build realistic mock Match objects. Each match needs: `id`, `slug`, `homeTeamId`, `awayTeamId`, `result: { home, away }`, `status`, `date` (Firestore Timestamp-like object or Date), `stadium`, `phase`, `pointsCalculated` flag. Use `{ toDate: () => new Date() }` for the date field to match Firestore Timestamp shape.

    Stories:
    - **GroupedByPhase:** 6 matches across group, round-of-16, quarterfinal phases — shows phase-grouped sections with match cards
    - **MixedStatuses:** Matches with various statuses (scheduled, live, finished, postponed) — status badges display correctly
    - **WithResults:** Finished matches with populated `result.home` and `result.away` scores — shows "Result: X - Y" text
    - **PointsCalculated:** Some matches with `pointsCalculated: true` — shows "✓ Points calculated" badge
    - **EditingMatch:** Wrapper that sets `editingMatchId` to the first match's ID — shows the `AdminMatchResultForm` inline (replaces Edit button)
  </action>
  <verify>
    <automated>pnpm build-storybook 2>&1 | tail -3</automated>
  </verify>
  <done>All AdminMatchList stories render in Storybook</done>
</task>

<task type="auto">
  <name>Task 2: Create AdminMatchesPage stories</name>
  <files>
    src/components/organisms/AdminMatchesPage/AdminMatchesPage.stories.tsx
  </files>
  <action>
    Create `AdminMatchesPage.stories.tsx`.

    AdminMatchesPage is a full-page component that:
    - Checks `useAuthStore` for user auth and admin role
    - Loads matches from Firestore via `getDocs` + `collection`
    - Has states: auth loading, not logged in, not admin, loading matches, error, loaded

    Since this component depends on Firestore and auth, mock both:
    1. Use `useAuthStore.setState()` in stories to set user state
    2. The Firestore `getDocs` call uses the Firebase mock plugin already configured in `.storybook/main.ts` (`firebase-mock`), so Firestore calls resolve in Storybook without a real Firebase connection

    Stories:
    - **LoadingAuth:** Set `isAuthLoading: true` — shows "Verifying access..." text
    - **NotLoggedIn:** Set `user: null, isAuthLoading: false` — returns null (no render)
    - **NotAdmin:** Set `user: { uid: 'test', email: 'test@test.com', role: 'user' }` — returns null (no render)
    - **Loaded:** Set `user: { uid: 'admin', email: 'admin@test.com', role: 'admin' }` and manually seed `matches` state. Since the component fetches from Firestore, create a wrapper that overrides the internal `matches` state or mocks the Firestore response before render (use `decorator` pattern to set initial state on the component copy or use `jest.mock`-style replacement via the Firebase mock plugin)
  </action>
  <verify>
    <automated>pnpm build-storybook 2>&1 | tail -3</automated>
  </verify>
  <done>Both AdminMatchesPage stories render without errors</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `pnpm build-storybook` succeeds
- Stories render without Firestore auth errors (mocked)
</verification>

<success_criteria>
- 2 new `.stories.tsx` files created with all specified stories
- `pnpm build-storybook` passes
</success_criteria>

<output>
Create `.planning/phases/02-create-stories-for-all-possible-components-so-i-can-develop/02-06-SUMMARY.md` when done
</output>
