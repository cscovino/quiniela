---
phase: 02-stories
plan: 07
type: execute
wave: 2
depends_on: [02-03]
files_modified:
  - src/components/templates/AuthTemplate/AuthTemplate.stories.tsx
  - src/components/templates/PredictionsTemplate/PredictionsTemplate.stories.tsx
  - src/components/pages/AstroPageComponents.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "AuthTemplate story renders login and register modes"
    - "PredictionsTemplate story renders loading, auth-required, and predictor list states"
    - "Astro page component documentation explains the wrapper pattern for all 5 page components"
  artifacts:
    - path: "src/components/templates/AuthTemplate/AuthTemplate.stories.tsx"
      provides: "AuthTemplate stories for login/register toggle"
    - path: "src/components/templates/PredictionsTemplate/PredictionsTemplate.stories.tsx"
      provides: "PredictionsTemplate stories covering all views"
    - path: "src/components/pages/AstroPageComponents.md"
      provides: "Documentation for Astro page shell components"
  key_links:
    - from: "AuthTemplate.stories.tsx"
      to: "AuthTemplate.tsx"
      via: "import"
    - from: "PredictionsTemplate.stories.tsx"
      to: "PredictionsTemplate.tsx"
      via: "import"
---

<objective>
Create Storybook stories for template-level components and document Astro page components.

**Purpose:** AuthTemplate and PredictionsTemplate are the highest-level layout components that compose molecules and organisms. Their stories validate the complete composition works. The 5 Astro page components are simple shells that can't have standard stories, so document their pattern instead.
**Output:** 2 new `.stories.tsx` files + 1 documentation file
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/templates/AuthTemplate/AuthTemplate.tsx
@src/components/templates/PredictionsTemplate/PredictionsTemplate.tsx
@src/components/templates/PredictionTemplate/PredictionTemplate.stories.tsx
@src/components/templates/ProfileTemplate/ProfileTemplate.stories.tsx
@src/components/pages/AuthPage.astro
@src/components/pages/PredictionsPage.astro
@src/components/pages/ProfilePage.astro
@src/components/pages/RankingsPage.astro
@src/components/pages/TournamentPage.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthTemplate and PredictionsTemplate stories</name>
  <files>
    src/components/templates/AuthTemplate/AuthTemplate.stories.tsx
    src/components/templates/PredictionsTemplate/PredictionsTemplate.stories.tsx
  </files>
  <action>
    **AuthTemplate.stories.tsx:**
    - AuthTemplate wraps LoginForm and RegisterForm with a toggle, plus decorative PixelArt elements.
    - Requires `translations` object with nested `login` and `register` translation objects (matching LoginFormProps and RegisterFormProps interfaces).
    - Build a full translations object with realistic Spanish and English text for all keys.

    Stories:
    - **LoginMode:** `initialMode='login', locale='en'` — shows login form with decorations
    - **RegisterMode:** `initialMode='register', locale='en'` — shows register form
    - **SpanishLogin:** `initialMode='login', locale='es'` — verifies Spanish locale rendering
    - Use `parameters: { layout: 'fullscreen' }` since it's a page-level template

    **PredictionsTemplate.stories.tsx:**
    - PredictionsTemplate is a large component with complex internal state management (auth, predictor loading, prediction wizard).
    - Mock `useAuthStore` for auth states.
    - The component has views: loading, auth-required, predictor list, editor, delete, and wizard.

    Stories:
    - **Loading:** Set `isAuthLoading: true` — shows Spinner + loading text
    - **AuthRequired:** Set `user: null, isAuthLoading: false` — shows login-required message with login button
    - **PredictorListEmpty:** Set `user: { uid: 'u1', email: 'user@test.com' }` — shows PredictorList with empty state (no predictors loaded)
    - **PredictorListWithItems:** This is harder since the component fetches from Firestore internally. Create a wrapper that intercepts the predictor loading or mock the entire component's data flow. Simplest approach: create a story wrapper that overwrites the internal `predictorEntries` state after mount using a decorator, or provide mock predictor data via the Firebase mock plugin
  </action>
  <verify>
    <automated>pnpm build-storybook 2>&1 | tail -3</automated>
  </verify>
  <done>Both template story files compile and render in Storybook</done>
</task>

<task type="auto">
  <name>Task 2: Document Astro page component pattern</name>
  <files>
    src/components/pages/AstroPageComponents.md
  </files>
  <action>
    Create `src/components/pages/AstroPageComponents.md` documenting the 5 Astro page components. These are simple shell components that pass props (locale, translations, initialMode) to React template components with `client:load`. They cannot have Storybook stories because `@storybook/astro` does not exist.

    The documentation should explain:
    - The purpose of each Astro page component: AuthPage, PredictionsPage, ProfilePage, RankingsPage, TournamentPage
    - Their common pattern: import a React template, extract translations via `getTranslations(locale)`, render with `client:load`
    - The Props interface each page accepts (`locale`, optional component-specific props)
    - That testing is done via the underlying React template's Storybook stories (e.g., AuthTemplate.stories tests the AuthPage logic indirectly)
    - A quick reference table:
      | Astro Page | Delegates To | Props |
      |------------|-------------|-------|
      | AuthPage | AuthTemplate | locale, initialMode |
      | PredictionsPage | PredictionsTemplate | locale |
      | ProfilePage | ProfileTemplate | locale |
      | RankingsPage | RankingsTable + NavBar | locale |
      | TournamentPage | TournamentHeader + MatchList + GroupStandings | locale |
    - How to add a new page: create `.astro` file, import template, pass translations, use `client:load`

    Keep the doc concise (under 50 lines). This is reference material for component development, not a full guide.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create AstroPageComponents.md first</automated>
  </verify>
  <done>Documentation file created with clear reference table and pattern explanation</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `pnpm build-storybook` succeeds
- Both template stories render without console errors
</verification>

<success_criteria>
- 2 new `.stories.tsx` files and 1 `.md` doc file created
- All stories render in Storybook
- `pnpm build-storybook` passes
</success_criteria>

<output>
Create `.planning/phases/02-create-stories-for-all-possible-components-so-i-can-develop/02-07-SUMMARY.md` when done
</output>
