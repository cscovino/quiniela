---
phase: 02-stories
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/molecules/PointsChart/PointsChart.stories.tsx
  - src/components/molecules/Predictions/PredictionsUI.stories.tsx
  - src/components/molecules/Predictions/PredictionStepFinal.stories.tsx
  - src/components/molecules/AdminMatchResultForm/AdminMatchResultForm.stories.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "PointsChart story renders SVG chart with point data"
    - "PointsChart story shows empty state when no data"
    - "PredictionsUI stories render progress bar, feedback, and navigation sub-components"
    - "PredictionStepFinal story renders final phase form and best players form"
    - "AdminMatchResultForm story renders match result editing form"
  artifacts:
    - path: "src/components/molecules/PointsChart/PointsChart.stories.tsx"
      provides: "PointsChart stories for data and empty states"
    - path: "src/components/molecules/Predictions/PredictionsUI.stories.tsx"
      provides: "Sub-component stories for PredictionsProgress, Feedback, Navigation"
    - path: "src/components/molecules/Predictions/PredictionStepFinal.stories.tsx"
      provides: "PredictionStepFinalPhase and PredictionStepBestPlayers stories"
    - path: "src/components/molecules/AdminMatchResultForm/AdminMatchResultForm.stories.tsx"
      provides: "AdminMatchResultForm stories for editing match results"
  key_links:
    - from: "PointsChart.stories.tsx"
      to: "PointsChart.tsx"
      via: "import"
    - from: "PredictionsUI.stories.tsx"
      to: "PredictionsUI.tsx"
      via: "import"
    - from: "PredictionStepFinal.stories.tsx"
      to: "PredictionStepFinal.tsx"
      via: "import"
    - from: "AdminMatchResultForm.stories.tsx"
      to: "AdminMatchResultForm.tsx"
      via: "import"
---

<objective>
Create Storybook stories for molecule-level prediction and admin components.

**Purpose:** These molecules handle specific prediction flow interactions (progress tracking, step navigation, scoring forms) and admin match management. Stories isolate each sub-component for focused development and debugging.
**Output:** 4 new `.stories.tsx` files
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/molecules/PointsChart/PointsChart.tsx
@src/components/molecules/Predictions/PredictionsUI.tsx
@src/components/molecules/Predictions/PredictionStepFinal.tsx
@src/components/molecules/AdminMatchResultForm/AdminMatchResultForm.tsx
@src/components/molecules/MatchCard/MatchCard.stories.tsx
@src/components/molecules/ScoreDisplay/ScoreDisplay.stories.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PointsChart and PredictionsUI stories</name>
  <files>
    src/components/molecules/PointsChart/PointsChart.stories.tsx
    src/components/molecules/Predictions/PredictionsUI.stories.tsx
  </files>
  <action>
    **PointsChart.stories.tsx:**
    - PointsChart takes `data: PointEntry[]` (with `date`, `points`, `cumulative`, optional `matchId`) and `translations` object.
    
    Stories:
    - **WithData:** Array of 10+ point entries spanning several dates with varying points — renders SVG line chart with gradient fill
    - **SinglePoint:** One data entry — chart edge case
    - **Empty:** Empty data array — shows "no data" fallback
    - **RisingTrend:** Data showing cumulative points steadily increasing

    **PredictionsUI.stories.tsx:**
    - This file exports 3 separate components: `PredictionsProgress`, `PredictionsFeedback`, `PredictionsNavigation`.
    - Create separate stories for each component.

    **PredictionsProgress:**
    - **ManySteps:** 12 step labels (like a full group stage), `currentStep=3`, `submittedSteps={new Set([0,1,2])}` — shows compact variant
    - **FewSteps:** 4 step labels, `currentStep=1` — shows non-compact variant
    - **FirstStep:** currentStep=0, no submittedSteps
    - **LastStep:** currentStep=steps.length-1, some submitted

    **PredictionsFeedback:**
    - **Success:** `feedback={{ type: 'success', message: 'Group A saved!' }}`
    - **Error:** `feedback={{ type: 'error', message: 'Failed to save predictions' }}`
    - **None:** `feedback={null}` — renders nothing

    **PredictionsNavigation:**
    - **MiddleStep:** currentStep=2, totalSteps=5, `canAdvance=true`, `submittedSteps=new Set()`
    - **LastStep:** currentStep=4, totalSteps=5 — no "Next" button, only "Back"
    - **CannotAdvance:** `canAdvance=false` — Next button is disabled
    - **SubmittedStep:** `submittedSteps=new Set([2])`, currentStep=2 — no submit hint shown
  </action>
  <verify>
    <automated>pnpm build-storybook 2>&1 | tail -3</automated>
  </verify>
  <done>All PointsChart and PredictionsUI stories render in Storybook</done>
</task>

<task type="auto">
  <name>Task 2: Create PredictionStepFinal and AdminMatchResultForm stories</name>
  <files>
    src/components/molecules/Predictions/PredictionStepFinal.stories.tsx
    src/components/molecules/AdminMatchResultForm/AdminMatchResultForm.stories.tsx
  </files>
  <action>
    **PredictionStepFinal.stories.tsx:**
    - This file exports `PredictionStepFinalPhase` and `PredictionStepBestPlayers`.
    
    **PredictionStepFinalPhase:**
    - **WithTeams:** Array of 8+ teams with fifaCode and name, `onSubmit` as action
    - **Empty:** Empty teams array — shows "Teams will be available soon" (respects locale — test both `locale='en'` and `locale='es'`)
    - **WithExistingPrediction:** Pre-filled `existingPrediction` with first/second/third/fourth values
    - **Disabled:** `isDisabled=true`

    **PredictionStepBestPlayers:**
    - **Empty:** No existing prediction, `onSubmit` as action
    - **WithExisting:** Pre-filled `existingPrediction` with bestGoalkeeper and bestScorer
    - **Disabled:** `isDisabled=true`

    **AdminMatchResultForm.stories.tsx:**
    - Requires a `Match` object with `id`, `homeTeamId`, `awayTeamId`, `result`, `status`, `date`, etc.
    - Build a mock match object. Use `new Date()` for `date` and structure result as `{ home: null, away: null }`.

    Stories:
    - **ScheduledMatch:** Status "scheduled", no scores yet — shows status dropdown + save/cancel buttons
    - **FinishedWithScores:** Status "finished", `result: { home: 2, away: 1 }` — shows pre-filled scores
    - **Postponed:** Status "postponed" — no score fields shown
    - **ValidationError:** Create wrapper that triggers the "Scores are required for finished matches" error
  </action>
  <verify>
    <automated>pnpm build-storybook 2>&1 | tail -3</automated>
  </verify>
  <done>All PredictionStepFinal and AdminMatchResultForm stories render in Storybook</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `pnpm build-storybook` succeeds
- All story files compile and render
</verification>

<success_criteria>
- 4 new `.stories.tsx` files created with all specified stories
- `pnpm build-storybook` passes
</success_criteria>

<output>
Create `.planning/phases/02-create-stories-for-all-possible-components-so-i-can-develop/02-04-SUMMARY.md` when done
</output>
