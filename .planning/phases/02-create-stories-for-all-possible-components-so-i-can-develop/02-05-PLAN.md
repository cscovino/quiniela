---
phase: 02-stories
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/organisms/BestPlayersForm/BestPlayersForm.stories.tsx
  - src/components/organisms/FinalPhaseForm/FinalPhaseForm.stories.tsx
  - src/components/organisms/GroupPredictionForm/GroupPredictionForm.stories.tsx
  - src/components/organisms/KnockoutBracketForm/KnockoutBracketForm.stories.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "BestPlayersForm story renders goalkeeper and scorer text inputs"
    - "FinalPhaseForm story renders 4-position dropdown selectors"
    - "GroupPredictionForm story renders group ranking grids with position selects"
    - "KnockoutBracketForm story renders knockout bracket with TeamSelector components"
  artifacts:
    - path: "src/components/organisms/BestPlayersForm/BestPlayersForm.stories.tsx"
      provides: "BestPlayersForm stories for empty, filled, and disabled states"
    - path: "src/components/organisms/FinalPhaseForm/FinalPhaseForm.stories.tsx"
      provides: "FinalPhaseForm stories for team selection states"
    - path: "src/components/organisms/GroupPredictionForm/GroupPredictionForm.stories.tsx"
      provides: "GroupPredictionForm stories with group data"
    - path: "src/components/organisms/KnockoutBracketForm/KnockoutBracketForm.stories.tsx"
      provides: "KnockoutBracketForm stories with match data"
  key_links:
    - from: "BestPlayersForm.stories.tsx"
      to: "BestPlayersForm.tsx"
      via: "import"
    - from: "FinalPhaseForm.stories.tsx"
      to: "FinalPhaseForm.tsx"
      via: "import"
    - from: "GroupPredictionForm.stories.tsx"
      to: "GroupPredictionForm.tsx"
      via: "import"
    - from: "KnockoutBracketForm.stories.tsx"
      to: "KnockoutBracketForm.tsx"
      via: "import"
---

<objective>
Create Storybook stories for prediction form organisms.

**Purpose:** These organisms are the core prediction wizard — BestPlayersForm, FinalPhaseForm, GroupPredictionForm, and KnockoutBracketForm. Each handles a distinct phase of the prediction workflow. Stories let the user develop and debug each form's validation, team selection UI, and submission behavior in isolation.
**Output:** 4 new `.stories.tsx` files
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/organisms/BestPlayersForm/BestPlayersForm.tsx
@src/components/organisms/FinalPhaseForm/FinalPhaseForm.tsx
@src/components/organisms/GroupPredictionForm/GroupPredictionForm.tsx
@src/components/organisms/KnockoutBracketForm/KnockoutBracketForm.tsx
@src/components/organisms/PredictionForm/PredictionForm.stories.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BestPlayersForm and FinalPhaseForm stories</name>
  <files>
    src/components/organisms/BestPlayersForm/BestPlayersForm.stories.tsx
    src/components/organisms/FinalPhaseForm/FinalPhaseForm.stories.tsx
  </files>
  <action>
    **BestPlayersForm.stories.tsx:**
    - BestPlayersForm has `onSubmit` callback and optional `existingPrediction`, `isDisabled`.
    
    Stories:
    - **Empty:** Both fields empty, `onSubmit` as action — user can type player names into text inputs
    - **Filled:** Pre-filled `existingPrediction` with `bestGoalkeeper='Emiliano Martinez'` and `bestScorer='Kylian Mbappe'`
    - **Disabled:** `isDisabled=true` — both inputs disabled, button disabled
    - **OnlyGoalkeeper:** Only `existingPrediction.bestGoalkeeper` set (scorer empty)

    **FinalPhaseForm.stories.tsx:**
    - FinalPhaseForm takes `teams` array (4-32 teams with `fifaCode` and `name`), `onSubmit`, optional `existingPrediction`, `isDisabled`.
    - Build a mock teams array of ~8 realistic teams (Argentina, Brazil, France, Germany, Spain, England, Portugal, Netherlands).

    Stories:
    - **Empty:** 8 teams, no existing prediction — all 4 position dropdowns show "Select team..."
    - **PartiallyFilled:** Only first and second positions selected
    - **Complete:** All 4 positions filled — valid submission ready
    - **WithDuplicates:** Wrapper that sets two positions to the same team — triggers "Each team can only be selected once" warning
    - **Disabled:** `isDisabled=true` — all controls disabled
  </action>
  <verify>
    <automated>pnpm build-storybook 2>&1 | tail -3</automated>
  </verify>
  <done>All BestPlayersForm and FinalPhaseForm stories render in Storybook</done>
</task>

<task type="auto">
  <name>Task 2: Create GroupPredictionForm and KnockoutBracketForm stories</name>
  <files>
    src/components/organisms/GroupPredictionForm/GroupPredictionForm.stories.tsx
    src/components/organisms/KnockoutBracketForm/KnockoutBracketForm.stories.tsx
  </files>
  <action>
    **GroupPredictionForm.stories.tsx:**
    - GroupPredictionForm takes `groups: GroupForPrediction[]` (with `slug`, `name`, `teams` array), `onSubmit`, `existingBets: Set<string>`, `isDisabled`.
    - Build mock groups: Group A (4 teams), Group B (4 teams), Group C (4 teams).
    - `onSubmit` uses action. `existingBets` can be empty for available groups.

    Stories:
    - **ThreeGroups:** 3 available groups, no bets submitted — shows all groups with team rows and position selects
    - **OneGroupAvailable:** 2 groups, but `existingBets=new Set(['group-a'])` — only Group B and C shown
    - **AllSubmitted:** `existingBets` containing all group slugs — shows "All group predictions have been submitted" empty state
    - **WithValidationError:** Wrapper that submits empty form to trigger "Rank all teams" validation errors
    - **Disabled:** `isDisabled=true`

    **KnockoutBracketForm.stories.tsx:**
    - KnockoutBracketForm takes `matches: KnockoutMatch[]` (with `slug`, `phase`, `phaseLabel`, `homeTeam`, `awayTeam`, `predictionDeadline`), `onSubmit`, `existingBets`, `isDisabled`.
    - Build mock knockout matches: 4 Round of 16 matches and 2 Quarterfinal matches with `TeamSelector`.

    Stories:
    - **MultipleRounds:** 4 Round-of-16 matches grouped by phase — shows phase sections with TeamSelector for each
    - **TBDMatch:** One match where `homeTeam=null` and `awayTeam=null`, `tbdHome='Winner A'`, `tbdAway='Runner B'` — shows "Teams TBD" badge, no selector
    - **AllBetsSubmitted:** `existingBets` containing all match slugs — empty state
    - **Disabled:** `isDisabled=true`
  </action>
  <verify>
    <automated>pnpm build-storybook 2>&1 | tail -3</automated>
  </verify>
  <done>All GroupPredictionForm and KnockoutBracketForm stories render in Storybook</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `pnpm build-storybook` succeeds
- All story files compile without TypeScript errors
</verification>

<success_criteria>
- 4 new `.stories.tsx` files created with all specified stories
- `pnpm build-storybook` passes
</success_criteria>

<output>
Create `.planning/phases/02-create-stories-for-all-possible-components-so-i-can-develop/02-05-SUMMARY.md` when done
</output>
