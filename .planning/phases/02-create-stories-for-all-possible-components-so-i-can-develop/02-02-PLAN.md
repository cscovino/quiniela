---
phase: 02-stories
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/atoms/AuthGuard/AuthGuard.stories.tsx
  - src/components/atoms/Toast/Toast.stories.tsx
  - src/components/organisms/ToastProvider/ToastProvider.stories.tsx
  - src/components/organisms/PWAInstall/PWAInstall.stories.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "AuthGuard story shows loading state while auth is initializing"
    - "AuthGuard story shows locked state when user is not logged in"
    - "AuthGuard story shows children when user is logged in"
    - "Toast story renders all 4 toast types (success, error, info, warning)"
    - "ToastProvider story renders with multiple toasts"
    - "PWAInstall story renders install banner when beforeinstallprompt fires"
  artifacts:
    - path: "src/components/atoms/AuthGuard/AuthGuard.stories.tsx"
      provides: "AuthGuard stories for loading/locked/authenticated states"
    - path: "src/components/atoms/Toast/Toast.stories.tsx"
      provides: "ToastContainer stories for all notification types"
    - path: "src/components/organisms/ToastProvider/ToastProvider.stories.tsx"
      provides: "ToastProvider story wrapping ToastContainer"
    - path: "src/components/organisms/PWAInstall/PWAInstall.stories.tsx"
      provides: "PWAInstall story showing install banner"
  key_links:
    - from: "AuthGuard.stories.tsx"
      to: "AuthGuard.tsx"
      via: "import"
      pattern: "import.*AuthGuard"
    - from: "Toast.stories.tsx"
      to: "Toast.tsx"
      via: "import"
      pattern: "import.*Toast"
    - from: "ToastProvider.stories.tsx"
      to: "ToastProvider.tsx"
      via: "import"
      pattern: "import.*ToastProvider"
    - from: "PWAInstall.stories.tsx"
      to: "PWAInstall.tsx"
      via: "import"
      pattern: "import.*PWAInstall"
---

<objective>
Create Storybook stories for simple atom and utility components: AuthGuard, Toast, ToastProvider, PWAInstall.

**Purpose:** These are foundational utility components that appear across the app. AuthGuard wraps protected pages, Toast shows notifications, ToastProvider renders the toast container, PWAInstall shows the install banner. Stories let the user verify their behavior in isolation.
**Output:** 4 new `.stories.tsx` files
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/atoms/AuthGuard/AuthGuard.tsx
@src/components/atoms/Toast/Toast.tsx
@src/components/organisms/ToastProvider/ToastProvider.tsx
@src/components/organisms/PWAInstall/PWAInstall.tsx
@src/components/atoms/Button/Button.stories.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthGuard and Toast stories</name>
  <files>
    src/components/atoms/AuthGuard/AuthGuard.stories.tsx
    src/components/atoms/Toast/Toast.stories.tsx
  </files>
  <action>
    Create two story files following the project pattern (`@storybook/react-vite`, `tags: ['autodocs']`).

    **AuthGuard.stories.tsx:**
    - AuthGuard wraps `children` and checks auth state via `useAuthStore`. Since the store is backed by Zustand, mock the store state directly in each story by calling `useAuthStore.setState(...)` before render.
    - Use a `decorator` or `beforeEach` pattern to set the auth store state.
    
    Stories:
    - **Loading:** Set `isAuthLoading: true` — shows Spinner + loading message
    - **Locked:** Set `user: null, isAuthLoading: false` — shows login-required message with Login button
    - **Authenticated:** Set `user: { uid: 'test', email: 'test@test.com' }` — renders children (use a `<div>Protected content</div>` as children)
    - **CustomMessage:** Locked state with custom `message` and `loginUrl` props

    **Toast.stories.tsx:**
    - ToastContainer receives `toasts` array and `onDismiss` callback. Use `action('dismiss')` from `@storybook/addon-actions` for the callback.
    - Import `type { Toast }` from `./Toast` for the toast type.

    Stories:
    - **Success:** Single success toast
    - **Error:** Single error toast
    - **Info:** Single info toast
    - **Warning:** Single warning toast
    - **AllTypes:** All 4 types rendered together in a group
    - **Empty:** Empty toasts array — should render nothing (returns null)
    - **MultipleToasts:** 3 toasts of mixed types
  </action>
  <verify>
    <automated>pnpm build-storybook 2>&1 | tail -3</automated>
  </verify>
  <done>Both story files compile; all stories render in Storybook dev server</done>
</task>

<task type="auto">
  <name>Task 2: Create ToastProvider and PWAInstall stories</name>
  <files>
    src/components/organisms/ToastProvider/ToastProvider.stories.tsx
    src/components/organisms/PWAInstall/PWAInstall.stories.tsx
  </files>
  <action>
    **ToastProvider.stories.tsx:**
    - ToastProvider reads from `useToastStore` (Zustand). Mock the store state in a `decorator` or directly in the story.
    - Set `useToastStore.setState({ toasts: [...] })` before render.
    
    Stories:
    - **WithToasts:** Pre-populate the store with 3 toasts of different types — ToastProvider renders them via ToastContainer
    - **Empty:** No toasts — renders nothing (returns null)

    **PWAInstall.stories.tsx:**
    - PWAInstall listens for `beforeinstallprompt` event. In Storybook, this event won't fire naturally so the banner is hidden by default.
    - Create a story wrapper component that manually sets `showPrompt` state to `true` to demonstrate the install banner UI.
    - Alternatively, use a `decorator` that dispatches a synthetic `beforeinstallprompt` event with `preventDefault()` and `prompt()` stubs.

    Stories:
    - **Visible:** Show the install banner with "Install App" text and Install/Dismiss buttons (via wrapper that forces visibility)
    - **Hidden:** Default state — banner is not shown (no `beforeinstallprompt` fired)
  </action>
  <verify>
    <automated>pnpm build-storybook 2>&1 | tail -3</automated>
  </verify>
  <done>Both story files compile; all stories render in Storybook dev server</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `pnpm build-storybook` succeeds
- Each story renders without `ReferenceError` or import failures
</verification>

<success_criteria>
- 4 new `.stories.tsx` files created with all specified stories
- `pnpm build-storybook` passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/02-create-stories-for-all-possible-components-so-i-can-develop/02-02-SUMMARY.md` when done
</output>
