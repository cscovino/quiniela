---
phase: 01-refactor-design-system-to-use-rem-units-for-font-size-and-ot
plan: 05
type: execute
wave: 5
depends_on:
  - 01-01
files_modified:
  - src/components/organisms/NavBar/NavBar.css
  - src/components/organisms/NavBar/NavBar.tsx
autonomous: true
requirements:
  - REQ-10
  - REQ-11
  - REQ-12
  - REQ-13
  - REQ-14
  - REQ-09
user_setup: []

must_haves:
  truths:
    - "Mobile menu has dark backdrop overlay separating it from page content"
    - "Hacer Predicciones CTA button is visually separated from nav items"
    - "Mobile menu shows user displayName (not email) when available"
    - "Logout button is visually prominent in mobile menu"
    - "Active nav link in mobile menu has prominent highlight"
    - "Tournament name displayed prominently on page"
  artifacts:
    - path: "src/components/organisms/NavBar/NavBar.css"
      provides: "Mobile menu backdrop overlay, CTA repositioning, logout styling, active link highlight"
    - path: "src/components/organisms/NavBar/NavBar.tsx"
      provides: "displayName preference over email in mobile menu"
  key_links:
    - from: "NavBar.css"
      to: "NavBar.tsx"
      via: "nav-bar__mobile-menu.open class"
      pattern: "mobile-menu.*open"
    - from: "NavBar.tsx"
      to: "auth-store"
      via: "user.displayName"
      pattern: "userDisplayName"
---

<objective>
Improve mobile menu UX: backdrop overlay, CTA repositioning, displayName display, prominent logout, active link highlighting, and tournament name display.

Purpose: Mobile menu currently lacks visual separation from page content, CTA button is sandwiched between nav items, user display falls back to email, logout is hard to find, and active state is subtle.
Output: Updated NavBar.css, NavBar.tsx
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-RESEARCH.md
@src/components/organisms/NavBar/NavBar.css
@src/components/organisms/NavBar/NavBar.tsx
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile menu backdrop overlay</name>
  <read_first>
    - src/components/organisms/NavBar/NavBar.css (lines 211-226: current mobile menu styles, no backdrop)
    - src/styles/global.css (--z-overlay: 300, --bg-overlay tokens)
  </read_first>
  <files>
    src/components/organisms/NavBar/NavBar.css
  </files>
  <action>
    Add a backdrop overlay behind the mobile menu using CSS ::before pseudo-element on .nav-bar__mobile-menu. The backdrop should cover the entire viewport below the nav bar.

    Add to .nav-bar__mobile-menu:
    - `position: fixed` (change from current absolute positioning for the menu itself)
    - `top: 100%` — starts below the nav bar
    - `left: 0; right: 0; bottom: 0` — full width and height below nav
    - Add ::before pseudo-element for the backdrop:
      ```css
      .nav-bar__mobile-menu::before {
        content: '';
        position: fixed;
        inset: 0;
        top: 60px; /* below nav bar height */
        background: rgba(0, 0, 0, 0.6);
        backdrop-filter: blur(2px);
        z-index: calc(var(--z-overlay) - 1);
        opacity: 0;
        transition: opacity var(--duration-normal) var(--ease-out);
      }
      .nav-bar__mobile-menu.open::before {
        opacity: 1;
      }
      ```
    - The menu content itself should have `position: relative; z-index: var(--z-overlay)` to appear above the backdrop
    - Add click handler to close menu when clicking on backdrop area (add onClick to the mobile menu div that calls closeMenu when clicking the backdrop, not the menu content)

    Update NavBar.tsx: Add onClick handler to .nav-bar__mobile-menu div that closes the menu when the backdrop is clicked (event.target === event.currentTarget).
  </action>
  <acceptance_criteria>
    - NavBar.css .nav-bar__mobile-menu::before contains `background: rgba(0, 0, 0, 0.6)` or similar backdrop color
    - NavBar.css .nav-bar__mobile-menu::before contains `backdrop-filter: blur`
    - NavBar.css .nav-bar__mobile-menu.open::before contains `opacity: 1`
    - NavBar.tsx mobile menu div has onClick handler for backdrop close
    - `pnpm build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'nav-bar__mobile-menu::before' src/components/organisms/NavBar/NavBar.css && grep -q 'backdrop-filter' src/components/organisms/NavBar/NavBar.css && pnpm build</automated>
  </verify>
  <done>Mobile menu has dark semi-transparent backdrop overlay with blur effect that closes menu when clicked</done>
</task>

<task type="auto">
  <name>Task 2: Reposition CTA button, show displayName, improve logout and active link visibility</name>
  <read_first>
    - src/components/organisms/NavBar/NavBar.tsx (lines 160-206: mobile menu structure, CTA placement, user display)
    - src/components/organisms/NavBar/NavBar.css (lines 259-289: mobile user info, CTA styles)
    - src/store/auth-store.ts (user object structure with displayName field)
  </read_first>
  <files>
    src/components/organisms/NavBar/NavBar.tsx
    src/components/organisms/NavBar/NavBar.css
  </files>
  <action>
    Four improvements to the mobile menu:

    1. **CTA Repositioning (Req 11):** Move the "Hacer Predicciones" CTA button to the TOP of the mobile menu (before nav links), not at the bottom. In NavBar.tsx, move the nav-bar__mobile-cta div from after nav-bar__mobile-user to before nav-bar__mobile-links. Style it as a prominent full-width button with pixel-art styling.

    2. **displayName display (Req 12):** The current code already uses `userDisplayName` which prefers displayName over email (line 52: `user?.displayName || user?.email || ''`). Verify this is used in the mobile menu section (line 178). If the mobile menu uses a different variable, update it to use userDisplayName.

    3. **Prominent logout (Req 13):** Style the logout button in mobile menu as a danger/primary button instead of a plain text link. In NavBar.css, add styles for the logout button within .nav-bar__mobile-user:
       - `background: var(--color-error-bg)` or `background: var(--color-secondary-500)`
       - `color: var(--color-error)` or `color: var(--text-inverse)`
       - `border: var(--border-width-md) solid var(--color-error-border)`
       - `padding: var(--space-2) var(--space-4)`
       - `font-family: var(--font-pixel)`
       - Full width within the mobile user section

    4. **Active link highlight (Req 14):** Enhance .nav-bar__mobile-link--active to be more prominent:
       - Add `background: var(--color-accent-500)` instead of just border-color
       - Add `color: var(--text-inverse)` for high contrast
       - Add `border-left: var(--border-width-lg) solid var(--color-gold-500)` for a left accent bar
       - Add `font-weight: var(--font-weight-bold)` if available
  </action>
  <acceptance_criteria>
    - NavBar.tsx mobile menu renders CTA before nav links (nav-bar__mobile-cta appears before nav-bar__mobile-links in JSX)
    - NavBar.tsx mobile user section uses userDisplayName variable (not user.email directly)
    - NavBar.css .nav-bar__mobile-user button (logout) has distinct background color (not transparent)
    - NavBar.css .nav-bar__mobile-link--active has `background: var(--color-accent-500)` or similar prominent background
    - NavBar.css .nav-bar__mobile-link--active has left border accent
    - `pnpm build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'nav-bar__mobile-cta' src/components/organisms/NavBar/NavBar.tsx && grep -q 'userDisplayName' src/components/organisms/NavBar/NavBar.tsx && grep -q 'nav-bar__mobile-link--active' src/components/organisms/NavBar/NavBar.css && pnpm build</automated>
  </verify>
  <done>Mobile menu has CTA at top, shows displayName, prominent logout button, and highlighted active nav link</done>
</task>

<task type="auto">
  <name>Task 3: Display tournament name prominently</name>
  <read_first>
    - src/components/organisms/NavBar/NavBar.tsx (line 82-84: brand title with gradient text)
    - src/components/organisms/NavBar/NavBar.css (lines 47-55: nav-bar__title with gradient)
  </read_first>
  <files>
    src/components/organisms/NavBar/NavBar.tsx
    src/components/organisms/NavBar/NavBar.css
  </files>
  <action>
    The tournament name "FIFA World Cup 2026" should be displayed prominently. The current NavBar brand title uses translations.brandLabel which is configurable.

    Update NavBar.tsx to add a subtitle below the brand title showing the tournament name:
    - Add a new span element with class "nav-bar__tournament-name" below the existing nav-bar__title span
    - Content: "FIFA World Cup 2026" (hardcoded, not from translations — this is the official tournament name)
    - Style: smaller text, using var(--text-xs), color var(--text-secondary), centered below the brand title

    In NavBar.css, add styles for .nav-bar__tournament-name:
    - `font-family: var(--font-pixel)`
    - `font-size: var(--text-xs)`
    - `color: var(--text-secondary)`
    - `display: block`
    - `margin-top: var(--space-1)`
    - Only visible on mobile (hide at 1024px+ with display: none in desktop media query) — on desktop the brand title is sufficient
  </action>
  <acceptance_criteria>
    - NavBar.tsx contains span with class "nav-bar__tournament-name" with text "FIFA World Cup 2026"
    - NavBar.css contains `.nav-bar__tournament-name` styles
    - Tournament name is hidden on desktop (@media (min-width: 1024px) { display: none })
    - Tournament name uses var(--text-xs) font-size
    - `pnpm build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'nav-bar__tournament-name' src/components/organisms/NavBar/NavBar.tsx && grep -q 'nav-bar__tournament-name' src/components/organisms/NavBar/NavBar.css && pnpm build</automated>
  </verify>
  <done>Tournament name "FIFA World Cup 2026" displayed below brand title on mobile, hidden on desktop</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Auth store | user.displayName from Firebase Auth — trusted server-side data |
| Client → CSS | No trust boundary — static CSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Spoofing | user.displayName in mobile menu | mitigate | React auto-escapes displayName; no dangerouslySetInnerHTML used; displayName from Firebase Auth (server-verified) |
| T-01-08 | Tampering | CSS backdrop/z-index | mitigate | Static CSS, z-index tokens from global.css ensure proper layering |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed |
</threat_model>

<verification>
- Mobile menu has ::before backdrop with rgba background and backdrop-filter blur
- CTA button appears before nav links in mobile menu JSX
- Mobile user section uses userDisplayName
- Logout button has distinct danger styling
- Active mobile link has prominent background and left border accent
- Tournament name span present below brand title
- pnpm build succeeds
</verification>

<success_criteria>
- `grep -q 'nav-bar__mobile-menu::before' src/components/organisms/NavBar/NavBar.css` returns true
- `grep -q 'nav-bar__tournament-name' src/components/organisms/NavBar/NavBar.tsx` returns true
- `pnpm build` completes without errors
- `pnpm test:run` passes
</success_criteria>

<output>
Create `.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-05-SUMMARY.md` when done
</output>
