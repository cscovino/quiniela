---
phase: 01-refactor-design-system-to-use-rem-units-for-font-size-and-ot
plan: 08
type: execute
wave: 7
depends_on:
  - 01-01
  - 01-03
files_modified:
  - src/styles/global.css
  - src/components/templates/PredictionsTemplate/PredictionsTemplate.css
autonomous: true
requirements:
  - REQ-08
user_setup: []

must_haves:
  truths:
    - "Page uses vibrant tournament colors (gradients, accents) instead of flat dark theme"
    - "Visual hierarchy enhanced with WC26 brand gradients"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Enhanced gradient usage and color application"
    - path: "src/components/templates/HomeTemplate/HomeTemplate.css"
      provides: "Vibrant hero/header section with tournament gradients"
  key_links:
    - from: "global.css"
      to: "all template CSS files"
      via: "CSS gradient variables (--gradient-wc26-*)"
      pattern: "var\\(--gradient-wc26"
---

<objective>
Make page more vibrant with tournament colors using existing WC26 gradient tokens.

Purpose: Current page feels "boring" — flat dark theme without enough color variation. The design system already has excellent gradient tokens (--gradient-wc26-primary, --gradient-wc26-energy, --gradient-wc26-trophy, --gradient-wc26-celebration) that are underutilized.
Output: Updated global.css, template CSS files with gradient applications
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-RESEARCH.md
@src/styles/global.css
@src/components/organisms/NavBar/NavBar.css (already has gradient border — reference pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply WC26 tournament gradients to page headers and key sections</name>
  <read_first>
    - src/styles/global.css (lines 509-516: existing WC26 gradient tokens)
    - src/components/organisms/NavBar/NavBar.css (lines 8-14, 29-36: gradient border and underline — reference pattern)
    - src/components/templates/HomeTemplate/HomeTemplate.css (if exists, or find main page template CSS)
    - src/components/templates/PredictionsTemplate/PredictionsTemplate.css (current flat styling)
  </read_first>
  <files>
    src/styles/global.css
    src/components/templates/PredictionsTemplate/PredictionsTemplate.css
  </files>
  <action>
    Apply existing WC26 gradient tokens to make the page more vibrant. The NavBar already uses gradient borders effectively — extend this pattern to other key sections.

    In global.css, add a new utility class for gradient text:
    ```css
    .text-gradient-energy {
      background: var(--gradient-wc26-energy);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    .text-gradient-trophy {
      background: var(--gradient-wc26-trophy);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    ```

    In PredictionsTemplate.css:
    - Add gradient accent to the page header (h1): apply .text-gradient-energy or direct gradient background
    - Add gradient border-bottom to the section headers (.predictions-template__section-header) using the same pattern as NavBar's gradient border
    - Add subtle gradient background to completed progress steps (currently just bg-card-hover): use a subtle gradient from var(--color-success-bg) to transparent

    The goal is to add color accents without overwhelming the pixel-art aesthetic. Use gradients sparingly — on headers, active states, and success indicators only.
  </action>
  <acceptance_criteria>
    - global.css contains `.text-gradient-energy` or `.text-gradient-trophy` utility class
    - PredictionsTemplate.css h1 or header uses gradient (background: var(--gradient-wc26-*) or text-gradient class)
    - PredictionsTemplate.css section headers have gradient accent (border-image or ::after with gradient)
    - `pnpm build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'text-gradient' src/styles/global.css && grep -q 'gradient-wc26' src/components/templates/PredictionsTemplate/PredictionsTemplate.css && pnpm build</automated>
  </verify>
  <done>Page headers and key sections use WC26 tournament gradients for vibrant visual enhancement</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → CSS | No trust boundary — static CSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Tampering | CSS gradients | mitigate | Static CSS, no runtime modification |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed |
</threat_model>

<verification>
- Gradient utility classes exist in global.css
- PredictionsTemplate.css applies gradients to headers and section accents
- pnpm build succeeds
- Visual review confirms enhanced vibrancy without overwhelming pixel-art aesthetic
</verification>

<success_criteria>
- `grep -q 'text-gradient' src/styles/global.css` returns true
- `grep -q 'gradient-wc26' src/components/templates/PredictionsTemplate/PredictionsTemplate.css` returns true
- `pnpm build` completes without errors
- `pnpm test:run` passes
- Visual review: page has noticeable color variation and tournament branding
</success_criteria>

<output>
Create `.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-08-SUMMARY.md` when done
</output>
