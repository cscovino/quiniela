---
phase: 01-refactor-design-system-to-use-rem-units-for-font-size-and-ot
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - src/components/templates/PredictionsTemplate/PredictionsTemplate.css
  - src/components/molecules/Predictions/PredictionsUI.tsx
  - src/components/molecules/Predictions/PredictionStepGroup.tsx
  - src/components/molecules/Predictions/PredictionStepGroup.css
autonomous: true
requirements:
  - REQ-04
  - REQ-02
user_setup: []

must_haves:
  truths:
    - "Progress indicator uses compact layout on mobile (reduced vertical space)"
    - "Select dropdowns in PredictionStepGroup are properly sized on mobile"
    - "Horizontal scrolling is eliminated on all viewports >= 320px"
  artifacts:
    - path: "src/components/templates/PredictionsTemplate/PredictionsTemplate.css"
      provides: "Compact progress indicator styles, navigation button styling"
    - path: "src/components/molecules/Predictions/PredictionStepGroup.css"
      provides: "Mobile-friendly select dropdown styles"
  key_links:
    - from: "PredictionsTemplate.css"
      to: "PredictionsUI.tsx"
      via: "predictions-template__progress--compact class"
      pattern: "progress--compact"
    - from: "PredictionStepGroup.css"
      to: "PredictionStepGroup.tsx"
      via: "prediction-step-group__select class"
      pattern: "prediction-step-group__select"
---

<objective>
Improve mobile UX: compact progress indicator, properly sized select dropdowns, and verify horizontal scroll elimination.

Purpose: Mobile users experience cramped progress indicators (too much vertical scrolling), native select dropdowns that are hard to tap, and potential horizontal overflow from grid layouts.
Output: Updated PredictionsTemplate.css, PredictionStepGroup.css
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-RESEARCH.md
@src/components/templates/PredictionsTemplate/PredictionsTemplate.css
@src/components/molecules/Predictions/PredictionsUI.tsx
@src/components/molecules/Predictions/PredictionStepGroup.tsx
@src/components/molecules/Predictions/PredictionStepGroup.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make progress indicator compact on mobile</name>
  <read_first>
    - src/components/templates/PredictionsTemplate/PredictionsTemplate.css (lines 100-168: progress indicator styles, compact variant at lines 109-122)
    - src/components/molecules/Predictions/PredictionsUI.tsx (line 19: isCompact = stepLabels.length > 8)
  </read_first>
  <files>
    src/components/templates/PredictionsTemplate/PredictionsTemplate.css
  </files>
  <action>
    The current compact variant (lines 109-122) only activates when stepLabels.length > 8. Make it ALWAYS compact on mobile viewports (< 480px) regardless of step count.

    Add a media query to PredictionsTemplate.css:
    ```css
    @media (max-width: 480px) {
      .predictions-template__progress {
        flex-direction: row;
        gap: var(--space-1);
      }
      .predictions-template__progress-step {
        padding: var(--space-1) var(--space-2);
        flex: 0 0 auto;
      }
      .predictions-template__progress-label {
        display: none;
      }
      .predictions-template__progress-number {
        width: 20px;
        height: 20px;
        font-size: var(--text-xs);
      }
    }
    ```
    This hides step labels on mobile (only numbers visible), reduces padding, and uses smaller number badges. Labels reappear at 480px+ via the existing desktop styles.

    Also style the Back/Next navigation buttons to use the Button component's pixelated corner styles. Add to the navigation buttons:
    - `font-family: var(--font-pixel)`
    - `border: var(--border-width-md) solid`
    - `padding: var(--space-2) var(--space-4)`
    - Apply the same clip-path polygon from Button.css for pixelated corners
  </action>
  <acceptance_criteria>
    - PredictionsTemplate.css contains `@media (max-width: 480px)` targeting `.predictions-template__progress`
    - Mobile progress hides labels (`.predictions-template__progress-label { display: none }` inside mobile media query)
    - Mobile progress uses row layout with reduced gap
    - Navigation buttons (.predictions-template__nav-btn) have `font-family: var(--font-pixel)`
    - Navigation buttons have clip-path for pixelated corners
    - `pnpm build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>grep -q '@media (max-width: 480px)' src/components/templates/PredictionsTemplate/PredictionsTemplate.css && grep -q 'predictions-template__nav-btn' src/components/templates/PredictionsTemplate/PredictionsTemplate.css && pnpm build</automated>
  </verify>
  <done>Progress indicator is compact on mobile (numbers only, row layout), navigation buttons have pixel-art styling</done>
</task>

<task type="auto">
  <name>Task 2: Fix mobile select dropdown sizing and positioning in PredictionStepGroup</name>
  <read_first>
    - src/components/molecules/Predictions/PredictionStepGroup.tsx (lines 315-329: native <select> for position ranking)
    - src/components/molecules/Predictions/PredictionStepGroup.css (existing styles, look for prediction-step-group__select)
  </read_first>
  <files>
    src/components/molecules/Predictions/PredictionStepGroup.css
    src/components/molecules/Predictions/PredictionStepGroup.tsx
  </files>
  <action>
    The native <select> in PredictionStepGroup (line 315) has minimal styling and is hard to tap on mobile. Add proper mobile-friendly styles:

    In PredictionStepGroup.css, add styles for .prediction-step-group__select:
    - `width: 100%` — full width for easy tapping
    - `min-height: 44px` — meets minimum touch target size (WCAG 2.5.5)
    - `padding: var(--space-2) var(--space-3)` — comfortable padding
    - `font-family: var(--font-pixel)` — consistent with design system
    - `font-size: var(--text-sm)` — readable size
    - `background: var(--bg-input)` — matches design system
    - `border: var(--border-width-md) solid var(--border-color)` — pixel-art border
    - `color: var(--text-primary)` — readable text
    - `appearance: none` — remove native styling for consistent look
    - Add custom dropdown arrow via background-image or ::after pseudo-element

    Add a mobile-specific media query for screens < 480px:
    - Increase min-height to 48px for easier tapping
    - Use full-width layout (already set)
    - Ensure the select doesn't overflow its container with `max-width: 100%`
  </action>
  <acceptance_criteria>
    - PredictionStepGroup.css contains `.prediction-step-group__select` with `min-height: 44px` or greater
    - PredictionStepGroup.css contains `appearance: none` for the select
    - PredictionStepGroup.css has `@media (max-width: 480px)` rule for select
    - Select has `font-family: var(--font-pixel)` and `border: var(--border-width-md)`
    - `pnpm build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'prediction-step-group__select' src/components/molecules/Predictions/PredictionStepGroup.css && grep -q 'min-height' src/components/molecules/Predictions/PredictionStepGroup.css && pnpm build</automated>
  </verify>
  <done>Select dropdowns in prediction step group have proper sizing (min 44px touch target), pixel-art styling, and mobile-friendly layout</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → CSS | No trust boundary — static CSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | CSS media queries | mitigate | Static CSS, no runtime modification |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed |
</threat_model>

<verification>
- Progress indicator uses compact layout on mobile (< 480px)
- Progress labels hidden on mobile, visible on desktop
- Navigation buttons have pixel-art styling with clip-path
- Select dropdowns have min-height 44px touch targets
- Select dropdowns use pixel-art styling (font, border, background)
- pnpm build succeeds
</verification>

<success_criteria>
- `@media (max-width: 480px)` targets progress indicator in PredictionsTemplate.css
- `.prediction-step-group__select` has min-height >= 44px
- `pnpm build` completes without errors
- `pnpm test:run` passes
</success_criteria>

<output>
Create `.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-04-SUMMARY.md` when done
</output>
