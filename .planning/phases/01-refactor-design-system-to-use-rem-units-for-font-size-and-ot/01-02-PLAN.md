---
phase: 01-refactor-design-system-to-use-rem-units-for-font-size-and-ot
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/hooks/usePredictionSteps.tsx
  - src/components/molecules/TeamSelector/TeamSelector.tsx
  - src/components/molecules/TeamSelector/TeamSelector.test.tsx
autonomous: true
requirements:
  - REQ-05
  - REQ-06
user_setup: []

must_haves:
  truths:
    - "Best Players step is NOT marked as submitted after completing Group A"
    - "TeamSelector shows the selected team name/flag after selection"
  artifacts:
    - path: "src/hooks/usePredictionSteps.tsx"
      provides: "Fixed step index calculation for final phase and best players submission"
      exports: ["usePredictionSteps"]
    - path: "src/components/molecules/TeamSelector/TeamSelector.tsx"
      provides: "Unique radio group name per TeamSelector instance"
  key_links:
    - from: "src/hooks/usePredictionSteps.tsx"
      to: "submittedSteps Set<number>"
      via: "setSubmittedSteps with correct stepIndex"
      pattern: "setSubmittedSteps.*add"
    - from: "src/components/molecules/TeamSelector/TeamSelector.tsx"
      to: "Radio name prop"
      via: "unique name per instance"
      pattern: "Radio.*name="
---

<objective>
Fix two critical bugs: Best Players false submission and TeamSelector empty selection display.

Purpose: These bugs block the core prediction flow. Best Players bug causes incorrect progress state; TeamSelector bug prevents knockout predictions from working.
Output: Fixed usePredictionSteps.tsx, TeamSelector.tsx, updated tests
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-RESEARCH.md
@src/hooks/usePredictionSteps.tsx
@src/components/molecules/TeamSelector/TeamSelector.tsx
@src/components/molecules/TeamSelector/TeamSelector.test.tsx
@src/components/atoms/Radio/Radio.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Best Players false submission bug (hardcoded step index)</name>
  <read_first>
    - src/hooks/usePredictionSteps.tsx (lines 180-250: handleFinalPhaseSubmit adds index 2, handleBestPlayersSubmit adds index 3 — both hardcoded)
    - src/hooks/usePredictionSteps.tsx (lines 353-476: useMemo builds steps dynamically, stepIndex counter increments per group/knockout/final/best-players)
    - src/hooks/usePredictionSteps.tsx (lines 253-312: handleGroupStepSubmit correctly receives stepIndex as parameter)
  </read_first>
  <files>
    src/hooks/usePredictionSteps.tsx
  </files>
  <action>
    The root cause: handleFinalPhaseSubmit hardcodes `setSubmittedSteps((prev) => new Set(prev).add(2))` and handleBestPlayersSubmit hardcodes `setSubmittedSteps((prev) => new Set(prev).add(3))`. But the actual step indices are dynamic — they depend on how many groups (12) and knockout rounds (6) exist. Best Players is at index 19 (12 + 6 + 1), not 3.

    Fix: Calculate the correct step index inside each handler using the same logic as the useMemo step builder. The final phase step index = groups.length + knockout_rounds_count. The best players step index = groups.length + knockout_rounds_count + 1.

    In handleFinalPhaseSubmit (line ~193):
    - Replace `setSubmittedSteps((prev) => new Set(prev).add(2))` with:
      `const finalPhaseIndex = groups.length + KNOCKOUT_PHASES.length; setSubmittedSteps((prev) => new Set(prev).add(finalPhaseIndex))`

    In handleBestPlayersSubmit (line ~229):
    - Replace `setSubmittedSteps((prev) => new Set(prev).add(3))` with:
      `const bestPlayersIndex = groups.length + KNOCKOUT_PHASES.length + 1; setSubmittedSteps((prev) => new Set(prev).add(bestPlayersIndex))`

    Note: KNOCKOUT_PHASES is already imported from '@utils/predictions-flow' at line 7. groups is available from the closure (line 63).
  </action>
  <acceptance_criteria>
    - usePredictionSteps.tsx does NOT contain `.add(2)` or `.add(3)` in setSubmittedSteps calls
    - usePredictionSteps.tsx contains `groups.length + KNOCKOUT_PHASES.length` in handleFinalPhaseSubmit
    - usePredictionSteps.tsx contains `groups.length + KNOCKOUT_PHASES.length + 1` in handleBestPlayersSubmit
    - `pnpm test:run` passes (existing tests still pass)
  </acceptance_criteria>
  <verify>
    <automated>grep -c '\.add(2)\|\.add(3)' src/hooks/usePredictionSteps.tsx | grep -q '^0$' && pnpm test:run</automated>
  </verify>
  <done>Final phase and best players submission use dynamically calculated step indices based on actual groups and knockout rounds count</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix TeamSelector empty selection (shared radio name collision)</name>
  <read_first>
    - src/components/molecules/TeamSelector/TeamSelector.tsx (line 42: Radio name="team-selector" hardcoded for ALL instances)
    - src/components/atoms/Radio/Radio.tsx (Radio component accepts name prop, passes through to input)
    - src/components/molecules/TeamSelector/TeamSelector.test.tsx (existing tests, no test for multiple instances)
  </read_first>
  <files>
    src/components/molecules/TeamSelector/TeamSelector.tsx
    src/components/molecules/TeamSelector/TeamSelector.test.tsx
  </files>
  <action>
    Root cause: Every TeamSelector instance uses `name="team-selector"` on its Radio inputs (line 42). When multiple TeamSelectors exist on the same page (e.g., in knockout predictions), they share the same radio group, causing selections in one to deselect in another.

    Fix: Use React.useId() to generate a unique name per TeamSelector instance. In TeamSelector.tsx:
    1. Add `const uniqueId = React.useId()` at the top of the component function
    2. Replace `name="team-selector"` with `name={`team-selector-${uniqueId}`}`

    Add a test to TeamSelector.test.tsx that renders two TeamSelector instances and verifies that selecting a team in one does NOT deselect the team in the other. Test should:
    - Render two TeamSelectors with different options (ARG/FRA and BRA/GER)
    - Select ARG in first selector
    - Select BRA in second selector
    - Assert first selector still shows ARG as selected (has class team-selector__option--selected)
  </action>
  <acceptance_criteria>
    - TeamSelector.tsx contains `React.useId()` or `useId()` call
    - TeamSelector.tsx Radio name prop uses template literal with uniqueId (not hardcoded "team-selector")
    - TeamSelector.test.tsx contains test for "multiple instances do not interfere"
    - `pnpm test:run -- -t "TeamSelector"` passes
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'useId' src/components/molecules/TeamSelector/TeamSelector.tsx && pnpm test:run -- -t "TeamSelector"</automated>
  </verify>
  <done>Each TeamSelector instance has a unique radio group name, preventing cross-component selection interference</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Firestore | Prediction submissions authenticated via Firebase Auth |
| Client → Client state | submittedSteps Set is client-side only, no server trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing | usePredictionSteps submittedSteps | mitigate | Step indices are client-calculated from server-fetched groups/matches; server validates actual submissions |
| T-01-04 | Tampering | TeamSelector radio state | mitigate | React.useId() generates stable unique IDs; no user-controlled input affects radio grouping |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed |
</threat_model>

<verification>
- No hardcoded step indices (2, 3) in setSubmittedSteps calls
- Dynamic calculation uses groups.length + KNOCKOUT_PHASES.length
- TeamSelector uses unique radio names per instance
- Multiple TeamSelector test passes (no cross-interference)
- pnpm test:run passes
</verification>

<success_criteria>
- `grep -c '\.add(2)\|\.add(3)' src/hooks/usePredictionSteps.tsx` returns 0
- TeamSelector.tsx contains useId() usage
- TeamSelector test for multiple instances passes
- `pnpm test:run` completes with 0 failures
</success_criteria>

<output>
Create `.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-02-SUMMARY.md` when done
</output>
