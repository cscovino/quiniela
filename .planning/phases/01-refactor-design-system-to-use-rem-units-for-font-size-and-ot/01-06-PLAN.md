---
phase: 01-refactor-design-system-to-use-rem-units-for-font-size-and-ot
plan: 06
type: execute
wave: 6
depends_on:
  - 01-01
  - 01-03
files_modified:
  - src/components/molecules/PredictorList/PredictorList.tsx
  - src/components/molecules/PredictorList/PredictorList.css
autonomous: true
requirements:
  - REQ-19
  - REQ-20
user_setup: []

must_haves:
  truths:
    - "Row click edits predictions, Edit button edits name/avatar (clearly distinguishable)"
    - "Edit/Delete buttons have visible pixel-art icons"
  artifacts:
    - path: "src/components/molecules/PredictorList/PredictorList.tsx"
      provides: "Pixel-art icons for Edit/Delete buttons, clear action labels"
    - path: "src/components/molecules/PredictorList/PredictorList.css"
      provides: "Action button styling with pixel-art icon support"
  key_links:
    - from: "PredictorList.tsx"
      to: "pixelarticons"
      via: "Icon component or CSS class for pixel icons"
      pattern: "pixelarticons|Icon"
---

<objective>
Improve predictor list UX: clarify row click vs edit button actions, add pixel-art icons to Edit/Delete buttons.

Purpose: Users confuse clicking a row (edits predictions) with clicking the Edit button (edits name/avatar). Edit/Delete buttons are plain text — need pixel-art icons for visual clarity.
Output: Updated PredictorList.tsx, PredictorList.css
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-RESEARCH.md
@src/components/molecules/PredictorList/PredictorList.tsx
@src/components/molecules/PredictorList/PredictorList.css
@src/components/atoms/Icon/Icon.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clarify row click vs edit button with visual distinction and labels</name>
  <read_first>
    - src/components/molecules/PredictorList/PredictorList.tsx (lines 58-113: row click handler, Edit/Delete buttons)
    - src/components/molecules/PredictorList/PredictorList.css (lines 60-83: action button styles)
  </read_first>
  <files>
    src/components/molecules/PredictorList/PredictorList.tsx
    src/components/molecules/PredictorList/PredictorList.css
  </files>
  <action>
    Make the distinction between row click (edit predictions) and Edit button (edit name/avatar) visually clear:

    In PredictorList.tsx:
    - Update the row's aria-label from `Select ${predictor.name}` to `Edit predictions for ${predictor.name}`
    - Update the Edit button's aria-label from `Edit ${predictor.name}` to `Edit name and avatar for ${predictor.name}`
    - Update the Edit button text from "Edit" to "Edit Profile" (more descriptive)
    - Add a visual hint on the row card: add a subtle right-arrow icon or "tap to edit predictions" text that appears on hover/focus

    In PredictorList.css:
    - Add a hover effect to .predictor-list__card that shows a cursor: pointer and subtle background change to indicate clickability
    - Add a ::after pseudo-element on .predictor-list__card:hover with content "→" positioned on the right side
    - Style .predictor-list__action-btn to look distinctly different from the row click — use a different background color (var(--bg-input)) and smaller size to indicate it's a secondary action
    - Add a visual separator between the card content and action buttons using a left border on .predictor-list__card-actions
  </action>
  <acceptance_criteria>
    - PredictorList.tsx row aria-label contains "Edit predictions for"
    - PredictorList.tsx Edit button aria-label contains "Edit name and avatar for"
    - PredictorList.tsx Edit button text is "Edit Profile" (not just "Edit")
    - PredictorList.css .predictor-list__card has hover effect with cursor: pointer
    - PredictorList.css .predictor-list__card-actions has visual separator (border-left or similar)
    - `pnpm test:run` passes
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Edit predictions for' src/components/molecules/PredictorList/PredictorList.tsx && grep -q 'Edit name and avatar' src/components/molecules/PredictorList/PredictorList.tsx && pnpm test:run</automated>
  </verify>
  <done>Row click clearly labeled as "edit predictions", Edit button labeled as "Edit Profile" for name/avatar changes</done>
</task>

<task type="auto">
  <name>Task 2: Add pixel-art icons to Edit/Delete buttons</name>
  <read_first>
    - src/components/molecules/PredictorList/PredictorList.tsx (lines 88-111: current Edit/Delete buttons)
    - src/components/atoms/Icon/Icon.tsx (available icon names)
    - package.json (pixelarticons dependency available)
  </read_first>
  <files>
    src/components/molecules/PredictorList/PredictorList.tsx
    src/components/molecules/PredictorList/PredictorList.css
  </files>
  <action>
    Replace plain text Edit/Delete buttons with pixel-art icons using the existing Icon component (or pixelarticons CSS classes).

    In PredictorList.tsx:
    - Import Icon component from '@atoms/Icon/Icon'
    - Replace Edit button content: use `<Icon name="edit" size={14} />` alongside "Edit Profile" text
    - Replace Delete button content: use `<Icon name="trash" size={14} />` alongside "Delete" text
    - If Icon component doesn't have "edit" or "trash" icons, use pixelarticons CSS classes: `<span className="pix pix-edit" />` and `<span className="pix pix-trash" />`

    In PredictorList.css:
    - Update .predictor-list__action-btn to accommodate icons: add `display: inline-flex`, `align-items: center`, `gap: var(--space-1)`
    - Add hover states that highlight the icon color
    - Ensure the danger button (.predictor-list__action-btn--danger) has appropriate icon color on hover
  </action>
  <acceptance_criteria>
    - PredictorList.tsx imports Icon component or uses pixelarticons CSS classes
    - Edit button contains icon element (Icon component or pix pix-edit class)
    - Delete button contains icon element (Icon component or pix pix-trash class)
    - PredictorList.css .predictor-list__action-btn has `display: inline-flex` and `align-items: center`
    - `pnpm test:run` passes
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Icon\|pix pix-' src/components/molecules/PredictorList/PredictorList.tsx && grep -q 'inline-flex' src/components/molecules/PredictorList/PredictorList.css && pnpm test:run</automated>
  </verify>
  <done>Edit and Delete buttons display pixel-art icons alongside text labels</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Firestore | Predictor edits authenticated via Firebase Auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Spoofing | predictor name in UI | mitigate | React auto-escapes; name from Firestore (server-verified) |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed — uses existing Icon component |
</threat_model>

<verification>
- Row aria-label clearly states "Edit predictions for"
- Edit button aria-label states "Edit name and avatar for"
- Edit button text is "Edit Profile"
- Edit/Delete buttons have pixel-art icons
- Action buttons have inline-flex layout with icon + text
- pnpm test:run passes
</verification>

<success_criteria>
- `grep -q 'Edit predictions for' src/components/molecules/PredictorList/PredictorList.tsx` returns true
- `grep -q 'Edit name and avatar' src/components/molecules/PredictorList/PredictorList.tsx` returns true
- `grep -q 'Icon\|pix pix-' src/components/molecules/PredictorList/PredictorList.tsx` returns true
- `pnpm test:run` completes with 0 failures
</success_criteria>

<output>
Create `.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-06-SUMMARY.md` when done
</output>
