---
phase: 01-refactor-design-system-to-use-rem-units-for-font-size-and-ot
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
files_modified:
  - src/components/atoms/Button/Button.css
  - src/components/atoms/Spinner/Spinner.css
  - src/components/atoms/Spinner/Spinner.tsx
  - src/components/molecules/TeamFlag/TeamFlag.tsx
  - src/components/molecules/TeamFlag/TeamFlag.css
autonomous: true
requirements:
  - REQ-03
  - REQ-16
  - REQ-01
user_setup: []

must_haves:
  truths:
    - "Back/Next buttons have pixelated stepped corners (not smooth border-radius)"
    - "Spinner displays pixel-art animation (not generic spinning border)"
    - "Team names show full name when container is wide enough, fallback to FIFA code when narrow"
  artifacts:
    - path: "src/components/atoms/Button/Button.css"
      provides: "clip-path polygon for pixelated stepped corners on all button variants"
    - path: "src/components/atoms/Spinner/Spinner.css"
      provides: "Pixel-art spinner using clip-path and steps() animation"
    - path: "src/components/molecules/TeamFlag/TeamFlag.css"
      provides: "Container query for responsive team name display"
  key_links:
    - from: "src/components/atoms/Button/Button.css"
      to: "all button instances"
      via: ".btn class selector"
      pattern: "\\.btn.*clip-path"
    - from: "src/components/molecules/TeamFlag/TeamFlag.css"
      to: "TeamFlag.tsx"
      via: "container-type: inline-size"
      pattern: "@container"
---

<objective>
Improve three components to match pixel-art design system: pixelated button corners, pixel-art spinner, and responsive team name display.

Purpose: Visual consistency with retro pixel art aesthetic. Button corners currently use no rounding (sharp) — need stepped pixel corners. Spinner is generic CSS border spinner — needs pixel-art style. Team names always truncate — need container-aware display.
Output: Updated Button.css, Spinner.css, Spinner.tsx, TeamFlag.tsx, TeamFlag.css
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-RESEARCH.md
@src/components/atoms/Button/Button.css
@src/components/atoms/Spinner/Spinner.css
@src/components/atoms/Spinner/Spinner.tsx
@src/components/molecules/TeamFlag/TeamFlag.tsx
@src/components/molecules/TeamFlag/TeamFlag.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pixelated stepped corners to Button component</name>
  <read_first>
    - src/components/atoms/Button/Button.css (current button styles, no border-radius used)
    - src/styles/global.css (design tokens: --shadow-accent, --shadow-md, etc.)
  </read_first>
  <files>
    src/components/atoms/Button/Button.css
  </files>
  <action>
    Add clip-path polygon for 4px stepped pixel corners to all button variants. The clip-path creates 2-step corners (4px + 4px) matching the pixel-art aesthetic. Use filter: drop-shadow() instead of box-shadow because clip-path clips box-shadows.

    Add to .btn base class:
    - `clip-path: polygon(0px 8px, 4px 8px, 4px 4px, 8px 4px, 8px 0px, calc(100% - 8px) 0px, calc(100% - 8px) 4px, calc(100% - 4px) 4px, calc(100% - 4px) 8px, 100% 8px, 100% calc(100% - 8px), calc(100% - 4px) calc(100% - 8px), calc(100% - 4px) calc(100% - 4px), calc(100% - 8px) calc(100% - 4px), calc(100% - 8px) 100%, 8px 100%, 8px calc(100% - 4px), 4px calc(100% - 4px), 4px calc(100% - 8px), 0px calc(100% - 8px))`

    Replace existing box-shadow declarations on each variant with filter: drop-shadow() equivalents:
    - .btn--primary: `filter: drop-shadow(4px 4px 0px var(--color-accent-600))`
    - .btn--secondary: `filter: drop-shadow(4px 4px 0px rgba(0, 0, 0, 0.15))`
    - .btn--accent: `filter: drop-shadow(4px 4px 0px var(--color-secondary-600))`
    - .btn--gold: `filter: drop-shadow(4px 4px 0px var(--color-gold-600))`
    - .btn--danger: `filter: drop-shadow(4px 4px 0px var(--color-error))`
    - .btn--ghost: no shadow (keep box-shadow: none)

    Keep existing hover transform effects (translate(-2px, -2px)) — they work with clip-path.
  </action>
  <acceptance_criteria>
    - Button.css .btn class contains `clip-path: polygon(`
    - Button.css .btn--primary contains `filter: drop-shadow(4px 4px 0px var(--color-accent-600))`
    - Button.css .btn--secondary contains `filter: drop-shadow(4px 4px 0px`
    - Button.css .btn--accent contains `filter: drop-shadow(4px 4px 0px var(--color-secondary-600))`
    - Button.css .btn--gold contains `filter: drop-shadow(4px 4px 0px var(--color-gold-600))`
    - Button.css .btn--danger contains `filter: drop-shadow(4px 4px 0px var(--color-error))`
    - `pnpm test:run -- -t "Button"` passes
    - `pnpm build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'clip-path: polygon(' src/components/atoms/Button/Button.css && grep -q 'filter: drop-shadow' src/components/atoms/Button/Button.css && pnpm test:run -- -t "Button"</automated>
  </verify>
  <done>All button variants have pixelated stepped corners via clip-path and drop-shadow instead of box-shadow</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Spinner to pixel-art style</name>
  <read_first>
    - src/components/atoms/Spinner/Spinner.css (current: border-based spinner with steps(8) animation)
    - src/components/atoms/Spinner/Spinner.tsx (component structure, role="status")
    - src/styles/global.css (--color-accent-500, --border-color tokens)
  </read_first>
  <files>
    src/components/atoms/Spinner/Spinner.css
    src/components/atoms/Spinner/Spinner.tsx
  </files>
  <action>
    Replace the current border-based spinner with a pixel-art style using clip-path and CSS animation. Keep the same sizes (sm: 16px, md: 24px, lg: 32px) and steps(8) animation timing.

    In Spinner.css:
    - Remove border-based styles (border, border-top-color)
    - Add background-color: var(--color-accent-500) to .spinner
    - Add clip-path polygon for pixelated circle shape:
      `clip-path: polygon(50% 0%, 60% 0%, 60% 10%, 70% 10%, 70% 20%, 80% 20%, 80% 30%, 90% 30%, 90% 40%, 100% 40%, 100% 60%, 90% 60%, 90% 70%, 80% 70%, 80% 80%, 70% 80%, 70% 90%, 60% 90%, 60% 100%, 40% 100%, 40% 90%, 30% 90%, 30% 80%, 20% 80%, 20% 70%, 10% 70%, 10% 60%, 0% 60%, 0% 40%, 10% 40%, 10% 30%, 20% 30%, 20% 20%, 30% 20%, 30% 10%, 40% 10%, 40% 0%)`
    - Add animation: `pixel-spin 0.8s steps(8) infinite`
    - Rename @keyframes from `pixel-spin` to `pixel-spin` (keep same name, it already exists)

    In Spinner.tsx:
    - Add aria-label="Loading" (already present)
    - No structural changes needed — the div element works as the spinner canvas
  </action>
  <acceptance_criteria>
    - Spinner.css .spinner contains `clip-path: polygon(`
    - Spinner.css .spinner contains `background: var(--color-accent-500)` or `background-color: var(--color-accent-500)`
    - Spinner.css does NOT contain `border-top-color`
    - Spinner.css contains `animation: pixel-spin` or `animation: ... steps(8)`
    - Spinner.tsx retains `role="status"` and `aria-label="Loading"`
    - `pnpm test:run -- -t "Spinner"` passes
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'clip-path: polygon(' src/components/atoms/Spinner/Spinner.css && ! grep -q 'border-top-color' src/components/atoms/Spinner/Spinner.css && pnpm test:run -- -t "Spinner"</automated>
  </verify>
  <done>Spinner displays pixel-art animated shape using clip-path instead of generic border spinner</done>
</task>

<task type="auto">
  <name>Task 3: Add responsive team name display with container queries</name>
  <read_first>
    - src/components/molecules/TeamFlag/TeamFlag.css (current: max-width based truncation, media queries at 480px/1024px)
    - src/components/molecules/TeamFlag/TeamFlag.tsx (current: shows name when showName=true, no FIFA code fallback)
  </read_first>
  <files>
    src/components/molecules/TeamFlag/TeamFlag.css
    src/components/molecules/TeamFlag/TeamFlag.tsx
  </files>
  <action>
    Use CSS container queries to show full team name when container is wide enough, fall back to FIFA code when narrow.

    In TeamFlag.css:
    - Add `container-type: inline-size` and `container-name: teamflag` to .team-flag base class
    - Add .team-flag__fifa-code element styling: `display: none` by default
    - Add @container teamflag (max-width: 119px) rule: hide .team-flag__name, show .team-flag__fifa-code
    - Add @container teamflag (min-width: 120px) rule: show .team-flag__name, hide .team-flag__fifa-code
    - Keep existing viewport media queries as fallback for browsers without container query support

    In TeamFlag.tsx:
    - Add a new span element for FIFA code: `<span className="team-flag__fifa-code">{fifaCode}</span>`
    - Place it alongside the existing name span (both present, CSS controls visibility)
    - The name span keeps its current logic (showName && name)
  </action>
  <acceptance_criteria>
    - TeamFlag.css contains `container-type: inline-size`
    - TeamFlag.css contains `container-name: teamflag`
    - TeamFlag.css contains `@container teamflag`
    - TeamFlag.css contains `.team-flag__fifa-code` class with display rules
    - TeamFlag.tsx renders a span with class `team-flag__fifa-code` containing fifaCode
    - `pnpm test:run -- -t "TeamFlag"` passes
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'container-type: inline-size' src/components/molecules/TeamFlag/TeamFlag.css && grep -q 'team-flag__fifa-code' src/components/molecules/TeamFlag/TeamFlag.tsx && pnpm test:run -- -t "TeamFlag"</automated>
  </verify>
  <done>Team names display full text in wide containers, fall back to FIFA code in narrow containers via CSS container queries</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → CSS | No trust boundary — static CSS, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | clip-path CSS | mitigate | Static CSS, no runtime modification; build process validates |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed |
</threat_model>

<verification>
- Button.css has clip-path polygon on .btn class
- Button.css uses filter: drop-shadow() instead of box-shadow on all variants
- Spinner.css uses clip-path polygon for pixel-art shape
- Spinner.css has no border-top-color
- TeamFlag.css has container-type: inline-size and @container rules
- TeamFlag.tsx renders team-flag__fifa-code span
- All component tests pass
</verification>

<success_criteria>
- `grep -q 'clip-path: polygon(' src/components/atoms/Button/Button.css` returns true
- `grep -q 'clip-path: polygon(' src/components/atoms/Spinner/Spinner.css` returns true
- `grep -q 'container-type: inline-size' src/components/molecules/TeamFlag/TeamFlag.css` returns true
- `pnpm test:run` passes with 0 failures
- `pnpm build` succeeds
</success_criteria>

<output>
Create `.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-03-SUMMARY.md` when done
</output>
