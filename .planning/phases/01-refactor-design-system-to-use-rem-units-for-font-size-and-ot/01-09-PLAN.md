---
phase: 01-refactor-design-system-to-use-rem-units-for-font-size-and-ot
plan: 09
type: execute
wave: 8
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
  - 01-05
  - 01-06
  - 01-07
  - 01-08
files_modified:
  - README.md
  - DESIGN.md
  - AGENTS.md
autonomous: true
requirements:
  - REQ-21
user_setup: []

must_haves:
  truths:
    - "README.md reflects current project state and Phase 1 changes"
    - "DESIGN.md documents updated design system (rem units, pixel-art components, PWA)"
    - "AGENTS.md updated with any new patterns established in Phase 1"
  artifacts:
    - path: "README.md"
      provides: "Project overview with Phase 1 feature list"
    - path: "DESIGN.md"
      provides: "Design system documentation with rem scale, pixel-art patterns, PWA setup"
    - path: "AGENTS.md"
      provides: "Updated project guidelines with Phase 1 patterns"
  key_links:
    - from: "DESIGN.md"
      to: "src/styles/global.css"
      via: "Design token references"
      pattern: "CSS variables|design tokens"
---

<objective>
Update project documentation to reflect Phase 1 changes: rem migration, bug fixes, component improvements, mobile UX, PWA features, and visual enhancements.

Purpose: Documentation must match current codebase state so future developers (and agents) have accurate reference material.
Output: Updated README.md, DESIGN.md, AGENTS.md
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@DESIGN.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with Phase 1 feature list and current state</name>
  <read_first>
    - README.md (current content, structure, feature list)
  </read_first>
  <files>
    README.md
  </files>
  <action>
    Update README.md to include:
    - Add a "Recent Changes" or "Changelog" section listing Phase 1 improvements
    - List all 21 requirements as completed features with brief descriptions
    - Update any outdated setup instructions
    - Ensure PWA install instructions are mentioned (if not already present)
    - Verify all command examples still work (pnpm dev, pnpm build, pnpm test, etc.)
    - Check that Lighthouse scores are still accurate (Accessibility 100, SEO 100, Best Practices 96)
  </action>
  <acceptance_criteria>
    - README.md contains a changelog or recent changes section mentioning Phase 1
    - README.md lists PWA install capability
    - README.md Lighthouse scores section present and accurate
    - README.md command examples match AGENTS.md commands
    - `pnpm lint` passes (no markdown lint errors if configured)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Phase 1\|rem units\|PWA install\|pixel-art' README.md</automated>
  </verify>
  <done>README.md accurately reflects current project state with Phase 1 changes documented</done>
</task>

<task type="auto">
  <name>Task 2: Update DESIGN.md with rem scale, pixel-art patterns, and PWA design</name>
  <read_first>
    - DESIGN.md (current content, design system documentation)
    - src/styles/global.css (design tokens to document)
    - src/components/atoms/Button/Button.css (pixelated corner pattern)
    - src/components/atoms/Spinner/Spinner.css (pixel-art spinner pattern)
  </read_first>
  <files>
    DESIGN.md
  </files>
  <action>
    Update DESIGN.md to document:
    - **rem Typography Scale:** Document all --text-* variables with their rem values and pixel equivalents. Note that all font-sizes use rem units (WCAG 2.2 compliance).
    - **Pixel-Art Component Patterns:** Document the clip-path polygon technique for stepped corners (Button component), the pixel-art spinner (clip-path + steps() animation), and the container query pattern for responsive team names (TeamFlag).
    - **PWA Design:** Document the PWA install prompt component (PWAInstall), manifest.json structure, service worker cache versioning pattern, and the beforeinstallprompt event flow.
    - **Mobile Menu:** Document the backdrop overlay pattern, CTA positioning, and active link highlighting.
    - **WC26 Gradients:** Document all --gradient-wc26-* tokens and their intended use cases.
    - **Container Queries:** Document the TeamFlag container query pattern as the standard for responsive component content.
  </action>
  <acceptance_criteria>
    - DESIGN.md contains "rem" or "Typography Scale" section documenting --text-* variables
    - DESIGN.md documents clip-path pixel corner technique
    - DESIGN.md documents pixel-art spinner pattern
    - DESIGN.md documents PWA install prompt and service worker versioning
    - DESIGN.md documents container query pattern for TeamFlag
    - DESIGN.md documents WC26 gradient tokens
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'rem\|Typography Scale' DESIGN.md && grep -q 'clip-path\|pixel' DESIGN.md && grep -q 'PWA\|service worker' DESIGN.md</automated>
  </verify>
  <done>DESIGN.md comprehensively documents rem scale, pixel-art patterns, PWA features, and container queries</done>
</task>

<task type="auto">
  <name>Task 3: Update AGENTS.md with Phase 1 patterns and conventions</name>
  <read_first>
    - AGENTS.md (current project guidelines)
    - .planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-RESEARCH.md (patterns established)
  </read_first>
  <files>
    AGENTS.md
  </files>
  <action>
    Update AGENTS.md to add Phase 1 patterns to the "Key Decisions" section:
    - All font-sizes use rem units with CSS variables (no hardcoded px)
    - Pixel-art corners use clip-path polygon (not border-radius)
    - TeamFlag uses CSS container queries for responsive name/FIFA code display
    - PWA install prompt uses beforeinstallprompt event with localStorage dismissal
    - Service worker cache versioning: bump CACHE_NAME in sw.js on each deploy
    - Mobile menu uses CSS ::before backdrop overlay pattern
    - Radio inputs in reusable components must use unique names (React.useId())
    - Prediction form grid collapses to single column at 480px breakpoint

    Also verify existing Key Decisions are still accurate and remove any that are no longer relevant.
  </action>
  <acceptance_criteria>
    - AGENTS.md Key Decisions section mentions rem units for font-sizes
    - AGENTS.md mentions clip-path for pixel-art corners
    - AGENTS.md mentions container queries for TeamFlag
    - AGENTS.md mentions PWA install prompt pattern
    - AGENTS.md mentions unique radio names with useId()
    - AGENTS.md mentions prediction form grid responsive breakpoint
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'rem units\|font-size' AGENTS.md && grep -q 'clip-path\|pixel-art' AGENTS.md && grep -q 'container.*query\|TeamFlag' AGENTS.md</automated>
  </verify>
  <done>AGENTS.md Key Decisions updated with all Phase 1 patterns and conventions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → Codebase | Documentation must match actual code — stale docs cause confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Tampering | Documentation files | mitigate | Documentation is static markdown; accuracy verified against source code |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed |
</threat_model>

<verification>
- README.md has changelog/recent changes section with Phase 1 items
- README.md Lighthouse scores accurate
- DESIGN.md documents rem typography scale
- DESIGN.md documents clip-path pixel corner technique
- DESIGN.md documents PWA install and service worker versioning
- DESIGN.md documents container query pattern
- AGENTS.md Key Decisions includes all Phase 1 patterns
- pnpm lint passes
</verification>

<success_criteria>
- `grep -q 'Phase 1\|rem units' README.md` returns true
- `grep -q 'rem\|Typography Scale' DESIGN.md` returns true
- `grep -q 'clip-path\|pixel' DESIGN.md` returns true
- `grep -q 'rem units\|font-size' AGENTS.md` returns true
- `grep -q 'clip-path\|pixel-art' AGENTS.md` returns true
- All documentation files exist and contain expected content
</success_criteria>

<output>
Create `.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-09-SUMMARY.md` when done
</output>
