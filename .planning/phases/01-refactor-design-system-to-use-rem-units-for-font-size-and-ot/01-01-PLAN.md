---
phase: 01-refactor-design-system-to-use-rem-units-for-font-size-and-ot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - public/manifest.json
  - src/components/molecules/TeamFlag/TeamFlag.css
  - src/components/templates/PredictionsTemplate/PredictionsTemplate.css
  - src/components/molecules/NavBar/NavBar.css
  - src/components/organisms/PredictionForm/PredictionForm.css
autonomous: true
requirements:
  - REQ-07
  - REQ-17
user_setup: []

must_haves:
  truths:
    - "All font-size values use rem units or CSS variables (no hardcoded px)"
    - "No horizontal scrolling occurs on any viewport width >= 320px"
    - "manifest.json contains 'id' field for Chrome 121+ PWA compatibility"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Base CSS with overflow-x prevention rules for img/video/table"
    - path: "public/manifest.json"
      provides: "PWA manifest with id field"
  key_links:
    - from: "src/styles/global.css"
      to: "all component CSS files"
      via: "CSS variable references (--text-*, --space-*)"
      pattern: "var\\(--text-|var\\(--space-"
---

<objective>
Migrate remaining hardcoded px font-sizes to rem units, eliminate horizontal scrolling root causes, and add PWA manifest id field.

Purpose: Foundation for all subsequent CSS changes. rem migration ensures WCAG 2.2 compliance; scroll fix prevents mobile UX breakage; manifest id enables PWA install prompt.
Output: Updated global.css, manifest.json
</objective>

<execution_context>
@/Users/cscovino/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/cscovino/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-RESEARCH.md
@src/styles/global.css
@public/manifest.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate remaining hardcoded px font-sizes to rem units</name>
  <read_first>
    - src/styles/global.css (CSS variable scale: --text-xs: 0.625rem, --text-sm: 0.75rem)
    - src/components/molecules/TeamFlag/TeamFlag.css (line 33: font-size: 9px)
    - src/components/templates/PredictionsTemplate/PredictionsTemplate.css (lines 122, 162, 248: font-size: 8px/9px)
    - src/components/organisms/NavBar/NavBar.css (line 144: font-size: 8px)
  </read_first>
  <files>
    src/components/molecules/TeamFlag/TeamFlag.css
    src/components/templates/PredictionsTemplate/PredictionsTemplate.css
    src/components/organisms/NavBar/NavBar.css
  </files>
  <action>
    Replace all hardcoded px font-size values with existing CSS variables from global.css. The rem scale is already defined: --text-xs (0.625rem = 10px), --text-sm (0.75rem = 12px). Specific replacements:
    - TeamFlag.css line 33: `font-size: 9px` → `font-size: var(--text-xs)` (10px, closest existing variable to 9px)
    - PredictionsTemplate.css line 122: `font-size: 8px` (compact progress label) → `font-size: var(--text-xs)`
    - PredictionsTemplate.css line 162: `font-size: 9px` (progress label) → `font-size: var(--text-xs)`
    - PredictionsTemplate.css line 248: `font-size: 9px` (team cell) → `font-size: var(--text-xs)`
    - NavBar.css line 144: `font-size: 8px` (notification badge) → `font-size: var(--text-xs)`
    After changes, run: `grep -rn 'font-size:.*[0-9]px' src/ --include='*.css' | grep -v 'var(--'` — must return 0 results.
  </action>
  <acceptance_criteria>
    - `grep -rn 'font-size:.*[0-9]px' src/ --include='*.css' | grep -v 'var(--'` returns 0 matches
    - TeamFlag.css contains `font-size: var(--text-xs)` for .team-flag__name
    - PredictionsTemplate.css contains `font-size: var(--text-xs)` for progress labels and team cells
    - NavBar.css contains `font-size: var(--text-xs)` for .nav-bar__badge
    - `pnpm test:run` passes
  </acceptance_criteria>
  <verify>
    <automated>grep -rn 'font-size:.*[0-9]px' src/ --include='*.css' | grep -v 'var(--' | wc -l | grep -q '^0$' && pnpm test:run</automated>
  </verify>
  <done>All font-size declarations in CSS files use CSS variables (var(--text-*)) instead of hardcoded px values</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate horizontal scrolling on mobile viewports</name>
  <read_first>
    - src/styles/global.css (lines 535-538: current html/body overflow rules)
    - src/components/organisms/PredictionForm/PredictionForm.css (line 19: minmax(340px, 1fr) grid)
    - src/components/templates/PredictionsTemplate/PredictionsTemplate.css (line 6: overflow-x: hidden)
  </read_first>
  <files>
    src/styles/global.css
    src/components/organisms/PredictionForm/PredictionForm.css
  </files>
  <action>
    Add global overflow prevention rules to global.css base styles section (after line 538). Add these rules:
    - `img, video, canvas, svg { max-width: 100%; height: auto; }` — prevents media from exceeding container width
    - `table { display: block; overflow-x: auto; -webkit-overflow-scrolling: touch; }` — makes tables horizontally scrollable within their container instead of breaking page layout

    Fix prediction-form__grid overflow: the grid uses `minmax(340px, 1fr)` which overflows on viewports < 340px. Add a media query to PredictionForm.css:
    ```css
    @media (max-width: 480px) {
      .prediction-form__grid {
        grid-template-columns: 1fr;
      }
    }
    ```
    This forces single-column layout on mobile, eliminating the 340px minimum width overflow.
  </action>
  <acceptance_criteria>
    - global.css contains `img, video, canvas, svg { max-width: 100%` rule
    - global.css contains `table { display: block; overflow-x: auto` rule
    - PredictionForm.css contains `@media (max-width: 480px)` with `.prediction-form__grid { grid-template-columns: 1fr }`
    - `pnpm build` succeeds (no CSS syntax errors)
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Global CSS prevents media overflow, tables scroll internally, and prediction form grid collapses to single column on mobile</done>
</task>

<task type="auto">
  <name>Task 3: Add PWA manifest id field for Chrome 121+ compatibility</name>
  <read_first>
    - public/manifest.json (current manifest structure, line 1-96)
  </read_first>
  <files>
    public/manifest.json
  </files>
  <action>
    Add `"id": "/"` as the second property in manifest.json (after "name", before "short_name"). This is required for Chrome 121+ to correctly identify the PWA and fire the beforeinstallprompt event. The value "/" matches the start_url and scope fields already present.
  </action>
  <acceptance_criteria>
    - manifest.json contains `"id": "/"` as a top-level property
    - manifest.json remains valid JSON (parseable)
    - `pnpm build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>node -e "const m = require('./public/manifest.json'); console.log(m.id === '/' ? 'OK' : 'FAIL')"</automated>
  </verify>
  <done>manifest.json includes "id": "/" property for Chrome 121+ PWA compatibility</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service Worker → Cache | sw.js caches static assets; version bumping prevents stale content |
| Client → CSS | No trust boundary — CSS is static, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | manifest.json | mitigate | manifest.json is static file served from Firebase Hosting; integrity ensured by build process |
| T-01-02 | Tampering | global.css | mitigate | CSS is static, no runtime injection; CSP meta tag in BaseLayout prevents inline style injection |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed — all work uses existing dependencies |
</threat_model>

<verification>
- All hardcoded px font-sizes eliminated from CSS (grep assertion)
- Horizontal scroll prevention rules present in global.css
- Prediction form grid responsive at 480px breakpoint
- manifest.json valid JSON with id field
- pnpm build succeeds with no errors
</verification>

<success_criteria>
- `grep -rn 'font-size:.*[0-9]px' src/ --include='*.css' | grep -v 'var(--'` returns 0 matches
- `pnpm build` completes without errors
- manifest.json parses as valid JSON with "id": "/" property
- No horizontal scroll on viewports >= 320px (verified by CSS rules)
</success_criteria>

<output>
Create `.planning/phases/01-refactor-design-system-to-use-rem-units-for-font-size-and-ot/01-01-SUMMARY.md` when done
</output>
